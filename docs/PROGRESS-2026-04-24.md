# M3 — Progress & Session Roster

**Date:** 2026-04-24
**Purpose:** Living index of planned coding/dev sessions (A onward), what's
shipped vs. still to do, plus a narrative summary of key discussions and
decisions that aren't already captured in `DESIGN.md` or `DEFERRED.md`.

This doc is the "where are we and why" companion to those two. When a
decision is made during a session that turns into a design principle or
persistent TBD, it goes into DESIGN.md or DEFERRED.md; ephemera and
running status live here. Full historical detail for sessions through
2026-04-23 is in `docs/archive/PROGRESS-2026-04-23.md`.

---

## Session roster

Status key: **✅ shipped** · **🚧 in flight** · **📋 planned** · **🧊 deferred**

| Session | Scope | Status | Version shipped | Commit |
|---|---|---|---|---|
| A–L, J, J-port, B-2, H-1, H-2, E-1a, E-1a2, E-1b, E-2a | Earlier sessions shipped through 2026-04-23. See `docs/archive/PROGRESS-2026-04-23.md` for full per-session detail. | ✅ shipped | see archive | see archive |
| E-2b | Simulation harness — MC tuning session. Deferred 2026-04-23 after E-2a playtest covered ~80–90% of what was being asked; not blocking downstream. Full findings + resumption notes in `docs/DEFERRED.md`. | 🧊 deferred | — | — |
| V-1 | **Memorize Mode scaffold.** Fork `platforms/tablet/match3-v11.11-tablet.jsx` → new platform `platforms/tablet-verses/`. Create `verses.html`, `src/entry-verses.jsx`, Vite config entry, "Verses" card on `index.html` landing page. Platform plays like tablet arcade; no memorize-specific logic yet — just plumbing. Ships `match3-v1.0-tablet-verses.jsx`. See `docs/DEFERRED.md` Memorize Mode entry for full scoping context. | ✅ shipped | v1.0 | `d20830b` |
| V-2 | **Memorize Mode MVP — single game (Titus 2:11–13 NKJV).** Deep-scoped 2026-04-24 (Q1–Q7 roadmap, ~13 locked decisions). Content model with `games/` + `_template/` + README; Vite `import.meta.glob` discovery; hardcoded Titus boot. First chunk pre-visible → moves = chunks − 1 = 12; default target `12 × 300 = 3,600`. Rolling 3-chunk text bar above board in Georgia serif (content typography — header stays Arial per v1.2 polish), two-column (reference flush-left / content indented), current emphasized. Reveal fires immediately on moves-decrement (v1.2 playtest finding — v1.1's settle-wait felt late); 250ms fade-in. Target-hit toast `Target reached!` + persistent header state-change (no 1.5× mode — that's V-4). Bonus-moves prompt force-disabled via `VERSES_MODE` guard. Start-of-round + end-of-round passage modal (v1.2 added start modal per playtest — memorization aid): dimmed-overlay, all 13 chunks in Georgia layout + translation label + `Begin game` / `Play again` button. Header trimmed in v1.3 — specials-on-board line + combo indicator hidden in VERSES_MODE so board + header + 3-chunk bar fit in a standard viewport. **See `docs/DEFERRED.md` V-2 entry for full spec.** Shipped iteratively within one session: v1.1 first pass → v1.2 (font / reveal-timing / text-bar / start-modal polish) → v1.3 (header trim). **Earliest playtest-able version.** | ✅ shipped | v1.3 | (this commit) |
| V-3a | **Memorize Mode — picker screen + back navigation + V-2 header shrink.** Outer `Match3Verses` wrapper manages `activeSlug` state; null = picker. Renamed v1.3 component → `VersesGame`, takes `slug` + `onBack` props, derives game/level via `useMemo`. New `VersesPicker` card grid (purple gradient, `repeat(auto-fill, minmax(220px, 1fr))`, "Verses" title + "Select a passage to begin." subtitle). Per card: title + translation + "N levels" for multi-level. `← Back` button in in-game header top-left + `Back` button on start-of-round modal — both call `onBack`. Bonus-moves init zeros + skips localStorage write in VERSES_MODE so picker exits leave no dead currency. Module-level `VERSES_BOOT_SLUG` / `versesActiveGame` / `versesActiveLevel` constants removed. V-2 header shrink bundled: VERSES_MODE drops `minHeight: 80px`, switches to `flex-start` + `gap: 6px`. Ships `match3-v1.4-tablet-verses.jsx`. | ✅ shipped | v1.4 | (this commit) |
| V-3b | **Memorize Mode — `levels[]` + level-select + multi-level play + Psalm 91 + in-memory hybrid progression.** Same-day ship after V-3a + scoping. Wrapper-state lift (Option B from V-3b decision #1): `Match3Verses` now owns `activeSlug`, `activeLevelIndex`, `completedLevels: Set<number>`, `fullCompleted: boolean`, and `bankedMoves`; renders one of `VersesPicker` / `VersesLevelSelect` / `VersesGame` (key on `slug:levelIndex` for fresh-mount per level). `versesFlattenLevel(game, levelIndex)` extended for multi-level. Hybrid progression wire-up: `VersesGame` calls `onLevelComplete(idx)` when target hit; wrapper updates `completedLevels` and flips `fullCompleted` when full. End-of-round button matrix per Decision #2 (Play again / Next level → / Retry / Replay / Back to {passage,level} selections / Arcade mode inactive). "Level N of M" inlined into header (one row — V-2 shrink preserved); instructions strip rewritten to drop dev jargon ("reveal more text" / "to see the full passage"). Psalm 91 content shipped at `games/psalm-91/game.js` (4 levels, NKJV, 49 chunks). | ✅ shipped | v1.5 | (this commit) |
| V-4 | **Memorize Mode — stars + persistence + victory round + arcade handoff (+ board shrink + see-entire-passage).** Six-decision scoping pass first (persistence shape / star rendering / card layout / victory round / arcade handoff / impl order), plus mid-V-4 fold-ins for board shrink and full-passage reveal. Wrapper-state `progress: Record<slug, GameProgress>` map hydrated from `m3_verses_<slug>` localStorage at mount; written via single useEffect on change. Stars: 5 × `★` Unicode (gold #FFD700 / unearned #ccc), thresholds 1.00/1.15/1.30/1.50/1.75. Picker cards add `X / Y ★` aggregate + inline ✓ when fullCompleted. Level-select cards add per-level star row + Best score + inline ✓ when completed. Silent 1.5× via existing `bonusRoundActive` flag (auto-flips on targetReached). Arcade mode button activates: writes bankedMoves to BANKED_KEY (additive) + carry-receipt key + navigates to tablet.html. Tablet arcade v11.12 reads receipt + 2.5s "+N bonus moves carried from memorize mode" banner + clears key. Board shrink in VERSES_MODE: 12 → 10 rows, text bar fonts 19/13 → 22/15px, gold left-border accent on current chunk. New `versesFlattenAllChunks` helper + "See entire <title>" 4th button on final-level win modal AND below level-select cards in free-replay. | ✅ shipped | v1.6 | (this commit) |
| V-5 | **Memorize Mode — admin + polish.** Originally scoped: admin panel (reveal-delay slider, simulate-completed, reset-stats, debug toggle) + final polish. **Deferred 2026-04-24 — "if needed."** Polish was rolled into V-2 → V-4 same-session as playtest signal arrived; only "simulate game completed" had standing value, and the cost of building an admin panel for one transient testing tool wasn't worth it. Hand-editing localStorage in DevTools covers the simulate-completed use case if it ever comes up. Re-open if a specific need surfaces. | 🧊 deferred (if needed) | — | — |
| I | Reward-mode integration — after Session H values settle. Integrate reward-round trigger into main tablet arcade (every-5-wins → next round runs in reward mode, uses existing `currentRun`) and campaign (per-level `rewardMode: true` flag in `levels/campaignConfig.js`). Wire the progressive special-drop lever (level 5+, scaling up per 5 levels, cap at level 30). Announcement copy: first-time explanation modal at arcade level 5; reward-round-entry full-screen banner. | 📋 planned | — | — |
| F | Desktop time-attack grace window (parity with campaign v1.25) + post-campaign arcade continuation (tablet arcade with campaign bonus-moves carried forward) + animated tutorials (inline mini-grid demos on each campaign intro screen). | 📋 planned | — | — |
| G | Campaign → next version — port Session C's hypernova rework to the campaign file. Straightforward mechanical port; pending playtest of tablet v11.7 first to confirm the feel. Version TBD (campaign is v1.26 after Session L). | 📋 planned | — | — |
| D-2 | Step-mode platform — sibling platform at `platforms/tablet-stepmode/`, admin-only, pipeline refactor from callback-setTimeouts → `await`-based pauses, "Next step →" button. Explicitly separate platform rather than a version bump because the refactor carries regression risk for normal play. | 🧊 deferred | — | — |
| Research | Match-3 literature survey — multi-session dive into academic / industry research on match-3 AI, balance, level difficulty prediction, design patterns. Includes monetization/compulsion-loop research as foreign-perspective reference. To run on a day when tokens are fresh and nothing else is scheduled. Scoping in `DEFERRED.md`. | 📋 planned | — | — |

---

## Current platform status

| Platform | Active version | Notes |
|---|---|---|
| Tablet arcade | v11.12 | Session V-4 — verses → arcade carry banner. Reads `m3_arcade_carry_from_verses` localStorage receipt at mount, shows 2.5s "+N bonus moves carried from memorize mode" top banner, clears the key. Otherwise identical to v11.11 (Session L contrast fix in admin panel + scoring-history drawer). |
| Campaign tablet | v1.26 | Session L contrast fix in CampaignAdmin + shared AdminPanel import |
| Reward-mode sandbox | v1.1 | Session H-2 — all 5 levers wired; HUGE-turn popup live; dev-time lever-fire log toggle |
| Tablet Sim | v1.3 | Session E-2a — Monte Carlo bot + Web Worker pool. `_SIM` + constants extracted to `simCore.js`. MC Parameters admin sub-section (N, depth-cap, rollout strategy, workers). |
| Tablet Verses | v1.6 | Session V-4 — stars + persistence + 1.5× victory + arcade handoff + 10-row board + "See entire" passage reveal. Wrapper-state `progress: Record<slug, GameProgress>` map hydrated from `m3_verses_<slug>` localStorage at mount; persisted via single `useEffect`. Stars: 5 × `★` Unicode (gold #FFD700 / unearned #ccc), thresholds 1.00/1.15/1.30/1.50/1.75; no-regression rule. Picker cards add `X / Y ★` aggregate + inline ✓ when fullCompleted. Level-select cards add per-level star row + Best score + inline ✓ when completed. Silent 1.5× via existing `bonusRoundActive` flag (auto-flips on targetReached); toast copy "✓ Target reached — 1.5×!"; persistent "· 1.5×" on header Target field. Arcade mode button activates: writes bankedMoves to BANKED_KEY (additive) + carry-receipt key + navigates to `tablet.html`. Board 12 → 10 rows in VERSES_MODE; text bar fonts 19/13 → 20/15px (post-playtest tune); reference column always 15px. New `versesFlattenAllChunks` + "See entire <title>" 4th button on final-level win modal AND below level-select cards in free-replay. |
| Desktop arcade | v12.1 | Needs bonus-moves update + timed-mode grace (Sessions F-ish) |
| Time Attack (desktop) | v12.1 | Needs grace window port from campaign v1.25 (Session F) |
| Phone 341 | v12.1 | Needs bonus-moves update (open since 2026-03-23 handoff) |
| Phone 418 | v12.4 | Session J-port: cap 99, run tracking, end-confirm, stacked bonus-moves banner |

---

## Rolling "what's next" suggestion

Updated after V-4 ship 2026-04-24. **The V-* track is effectively
complete.** V-4 was today's largest single session and the user's
read at end-of-day was that the game feels good. V-5 deferred to
"if needed" — polish was rolled into V-2 → V-4 inline, and the
remaining admin features didn't justify a session of their own.

The next memorize-mode work is **content** (more passages),
authored by the user, dropped into `platforms/tablet-verses/games/<slug>/`
with the existing data shape — no engine changes needed unless a
new passage surfaces a missing capability. Playtest of V-4 also
ongoing.

1. **More memorize-mode passages (user task).** Author additional `games/<slug>/game.js` files following the Titus and Psalm 91 patterns. The data shape supports both single-level (top-level `verses` array) and multi-level (`levels[]` array) games. Reference format short enough to fit the 110px text-bar column ("Ps. 91:N" worked; longer book names may need either abbreviation or column-width tuning). Per-level `targetScore` overrides for outliers; default is `moves × 300`.
2. **Playtest V-4** — Open `verses.html`. Walk Psalm 91 through to completion; verify stars award correctly at each threshold, best score persists across browser refresh, full-passage modal reveals on the final-level win modal AND on level-select after completion, arcade-mode handoff carries bonus moves to tablet arcade with the entry banner. Free-replay end-of-round shows Replay/Retry per outcome.
3. **V-5 if/when needed** — admin panel for simulate-completed / reset-stats. Re-open this session if a specific need surfaces; otherwise it stays deferred.
4. **Playtest V-3 (still open)** — 250ms chunk fade-in feel, 2.5s passage-modal hold pacing, header-shrink fit on smaller viewports.
4. **V-4 through V-5** — stars + persistence + 1.5× victory round + arcade handoff, then admin + polish. See roster for per-session details.
5. **Reward-mode playtest iteration** — ongoing user-driven tuning of tablet-rewardmode v1.1 (Session H-2 sandbox). No code session scoped yet.
6. **Device verification** of phone-418 v12.4 and tablet v11.11 on iPad (still open from 2026-04-22).
7. **Session I — reward-mode integration** — unblocked once reward-mode lever values settle.
8. **Session G — campaign hypernova port** — mechanical duplication of Session C.
9. **Session F bits** — desktop time-attack grace, post-campaign arcade continuation, animated tutorials.
10. **Research** — evening task with fresh tokens.
11. **Session E-2b — MC tuning** — deferred; see `docs/DEFERRED.md` E-2b entry for cold-resumption notes.

---

## Addendum — 2026-04-24 V-* track wrap

Five sessions shipped in one day across the V-* track:
- V-1 (`d20830b`) — scaffold v1.0
- V-2 (`1502953`) — single-game MVP, Titus 2:11–13 (v1.1 → v1.3)
- V-3a (`7690028`) — picker + back-nav (v1.4)
- V-3b (`968df9e`) — multi-level + Psalm 91 (v1.5)
- V-4 (`718a349`) — stars + persistence + 1.5× victory + arcade handoff + 10-row board + see-entire-passage (v1.6 + tablet v11.12)

User's end-of-day read: **the game feels good**. Standing plan from
here is playtest + content authoring (more passages). V-5 (admin
panel + final polish) deferred to "if needed" — polish was already
rolled into V-2 → V-4 inline, and the remaining admin features
didn't justify a session of their own. Hand-editing localStorage in
DevTools covers the one-off simulate-completed use case if it ever
comes up. Re-open if a specific need surfaces.

The next memorize-mode work is **content**, not code — additional
`platforms/tablet-verses/games/<slug>/game.js` files authored by
the user against the existing data shape. No engine changes
required unless a new passage surfaces a capability gap.

## Addendum — 2026-04-24 V-4 shipped (same-day after V-3b)

V-4 was today's largest single delivery — six-decision scoping pass
upfront, then 11 implementation steps with two mid-session
fold-ins (board shrink, see-entire-passage reveal). Full locked
spec lives in `docs/DEFERRED.md` V-4 entry; this addendum captures
implementation notes that don't belong in the spec.

**Architecture extension.** Wrapper-state pattern from V-3b extended
naturally — `progress` map joins the existing `activeSlug`,
`activeLevelIndex`, `completedLevels`, `fullCompleted`, `bankedMoves`.
Source of truth split: `progress` is persisted (long-lived, rehydrates
across sessions); `completedLevels` Set + `fullCompleted` boolean
are session mirrors that re-sync on game-pick. The dual-state pattern
preserves V-3b's Set-based level-select consumer contract while letting
V-4 layer persistence on top without a Set-vs-array refactor.

**handleLevelComplete signature change.** V-3b shipped with
`onLevelComplete(idx)`; V-4 extended to `onLevelComplete(idx, score,
target)`. Score + target are needed for the no-regression
star/best-score computation. The change is additive — V-3b call site
becomes a 4-arg call, no breaking removal.

**bankedMoves stays at the wrapper.** V-3b's lift held up — V-4 just
adds the arcade-mode handoff path that reads bankedMoves directly
from wrapper state. No further architecture change.

**Two same-session post-playtest tweaks.** Same-pattern as V-3b's
header-shrink + chunk-jargon fixes:
- **Text bar font sizes tightened.** v1.6 first pass was 22/15px main/prior
  + a gold left-border accent + tinted background on the current row,
  attempting to make the text bar more prominent (the v1.5 text bar
  was easy to ignore). Playtest reaction: 22px was *too* big, the
  reference (e.g., "Ps. 91:1") inflated alongside the chunk content
  unhelpfully, and the left-border + tint didn't add anything.
  Same-session retune: main 22 → 20px, reference always 15px (no
  longer scales with the current row), drop the left-border + tint
  entirely. Bigger font + bolder weight does the prominence work
  on its own.

**Persistence-layer pragmatism.** Eager-load all games from
localStorage at wrapper mount; persist all slugs on every progress
change. Small N at V-4 (2 games), so the inefficiency is invisible.
A future "many-games" world might need per-slug write tracking, but
optimizing for 2 games would be premature.

**Arcade-mode handoff is additive on BANKED_KEY.** Don't overwrite —
adds carried amount to the existing BANKED_KEY value. Player who has
prior arcade bonus moves stored from earlier play keeps them when
the carry lands.

**Star thresholds reuse campaign exactly.** `[1.00, 1.15, 1.30, 1.50,
1.75]`. `★` Unicode + `#FFD700` gold + `#ccc` unfilled — same
character, same colors as `core/AdminPanel.jsx` and the campaign
file. Visual consistency wins; no platform-specific star treatments.

**Bundle impact across V-4.** Verses 75.72 → 81.60 kB (+5.88 kB).
Tablet 64.37 → 65.26 kB (+0.89 kB for the v11.12 carry banner).
Build clean throughout.

**Open for V-4 playtest:**
- Star-threshold feel after persistence settles — does 1.75× target
  for 5 stars feel achievable on Psalm 91 levels (which have fewer
  cascade opportunities now with the 10-row board)?
- "Arcade mode" caption update needed — V-3b had "Available in v1.6"
  but now we're shipping v1.6, button is active.
- Star color contrast on the dim level-select cards in playtest.
- 1.5× scoring landing as expected — does the toast + persistent
  header indicator feel coherent with cascades that now multiply?

## Addendum — 2026-04-24 V-3b shipped (same-day after V-3a)

V-3b ran right after V-3a's push, with a five-decision scoping
pass first (state architecture / button matrix / locked-card
visual / level-select layout / implementation order). Full
locked spec lives in `docs/DEFERRED.md` V-3 entry; this addendum
captures the implementation notes that don't belong in the spec.

**Architecture — Option B locked.** Wrapper owns navigation +
cross-level state. `VersesGame` reduced to "play one level" with
slug+levelIndex props. Per-level remount via `key={slug:levelIndex}`
means per-level state (score, moves, target, revealedChunkIndex)
auto-resets without manual reset orchestration. Cross-level state
(bankedMoves, completedLevels, fullCompleted) survives across
remounts because it lives on the wrapper.

The `bankedMoves` lift was the largest mechanical cost in this
refactor (anticipated during V-3b decision #1 scoping). Done with
a small alias trick: inside `VersesGame`, `const setBankedMoves =
onBankedMovesChange;` — preserves all existing call sites without
mass rename. The functional updaters (`setBankedMoves(prev =>
Math.min(prev + n, CAP))`) keep working because React's useState
dispatch + the wrapper's setBankedMoves both accept updater
functions. ~half-dozen call sites total, none changed
syntactically.

**Two same-session post-playtest tweaks** during V-3b after
playing through the Psalm 91 Level 1 → Level 2 transition:
- **Header indicator inlined.** First pass put "Level N of M"
  on a second row under the game title, which made the header
  taller. Same problem V-2 fought with the specials-on-board
  line. Fix: inline as a small italic span after the title
  (`Psalm 91 · Level 1 of 4`), one row total.
- **Instructions strip dejargoned.** First pass kept V-2's
  "Match 3+ tiles to reveal each chunk" / "keep playing through
  all chunks." User flagged "chunk" as dev jargon. Fix:
  "reveal more text" / "to see the full passage." Standing
  reminder for content authoring: "chunk" stays in code,
  comments, and DEFERRED.md but never in user-facing strings.

**Bundle impact.** verses bundle 65.56 (v1.4) → 75.72 kB (v1.5)
across V-3b — +10.16 kB total for wrapper-state lift,
`VersesLevelSelect` component, multi-level play flow + matrix,
Psalm 91 content (~2.4 kB by itself). Build clean throughout.

**Open for V-3b playtest:**
- Free-replay UX feel (after full-game completion).
- "Arcade mode" inactive button caption tone — "Available in
  v1.6" might read internal-jargon-y; could revisit for V-4.
- Level-select card hover-lift feel against the picker hover.
- Locked-card visual aggressiveness (opacity 0.5 + grayscale
  0.7) — settle in playtest if the dimming reads strong enough
  vs. the unlocked siblings.

## Addendum — 2026-04-24 V-3a shipped

Single-pass implementation against the locked V-3 spec — no design
reversals during coding.

**Architecture choice — outer/inner split + key-based remount.**
Considered three approaches for picker ↔ game switching:
- **A.** Top-of-component early return for picker, keep all hooks
  running with placeholder state when no slug.
- **B.** Outer `Match3Verses` wrapper that conditionally renders
  `VersesPicker` or `VersesGame`, with `key={slug}` forcing
  fresh-mount of the game on each pick.
- **C.** Single component, rebuild board state via effect when
  slug changes.

Picked B. Reasons: cleanest separation of picker chrome from game
chrome (no shared style overrides); useState initializers can read
the slug prop synchronously at first render; `key={slug}` will
also handle V-3b's level-to-level remounts without changes to the
wrapper. The renamed `VersesGame` body is otherwise unchanged from
v1.3 — the only edits inside it are derivation (game/level from
slug prop), back-button wiring, and the header-shrink follow-up.

**Bonus-moves localStorage — small spec-driven side change.** V-3
spec calls for the bonus-moves pool to zero on any return to
picker. Naive remount-via-key was insufficient because the v1.3
useState initializer read from `BANKED_KEY` and the persist-to-
localStorage effect would have re-hydrated it on the next pick.
Two-line fix: in VERSES_MODE, init to 0 and short-circuit the
write effect. Memorize-mode bonus moves are now session-scoped
and never bleed into the shared key arcade-mode reads.

**Bundle impact.** verses bundle 65.56 → 68.30 kB (+2.74 kB) for
the picker component, outer wrapper, back-buttons, and the
useMemo derivations. No worker chunks. Build clean.

**Open for V-3a playtest:**
- Header fit (board + header + 3-chunk text bar without scroll)
  after `flex-start` + `gap: 6px` shrink.
- Picker subtitle copy ("Select a passage to begin." — alternates
  parked: "Memorize through play.", "Match tiles. Learn
  Scripture.", "Play to memorize.", or omit).
- Card hover-lift feel (`translateY(-2px)` + box-shadow lift).
- Card-grid spacing on smaller browser widths (`auto-fill`,
  220px min).

## Addendum — 2026-04-24 V-3 deep-scoping session

Ran right after V-2 shipped + playtested. Full locked spec lives in
`docs/DEFERRED.md` V-3 entry (canonical reference for V-3a + V-3b
coding). This addendum captures only the session meta: what was
decided, where reversals happened, and what was left open for
playtest-driven adjustment.

**Eight decisions worked one-per-message:**
1. Session scope → split V-3a (picker) + V-3b (multi-level + Psalm 91). Reason: V-2 ran three file versions in one session and left ~60% context behind; V-3 adds more surface and new content authoring.
2. Picker screen → card grid matching `index.html`; single-level cards = title + translation only; multi-level cards add `N levels`. Subtitle "Select a passage to begin." locked with a note that it's open to change (alternatives parked: "Memorize through play.", "Match tiles. Learn Scripture.", "Play to memorize.", or omit).
3. Level-select screen → same visual language, title-only per card pre-V-4; dimmed visual for locked levels; `← Back` in header.
4. Navigation → back buttons in three placements (in-game header, start modal, level-select header). Bonus-moves zeroes on return to main picker, persists between play ↔ level-select within same game. No mid-round confirm.
5. Hybrid progression at V-3 → in-memory (Option A). `completedLevels: Set` + `fullCompleted` flag. Session-scoped; refresh resets. V-4 wraps same state shape with localStorage.
6. Multi-level play UX → "Level N of M" indicator in header; start-modal fires at each level start; per-level state resets on level transition; bankedMoves + completedLevels persist across levels within a game.
7. Psalm 91 content → NKJV, 4 thematic levels (1–4 / 5–8 / 9–13 / 14–16), user authored chunks during scoping, 6 NKJV proofread fixes applied, reference format "Ps. 91:N" (abbreviated to fit 110px text-bar column — spelled-out "Psalm 91:16" estimated ~112px in Georgia italic at 19px, over the column width). Total 49 chunks / 45 moves across the 4 levels.
8. V-2 follow-up header-shrink → bundle into V-3a first commit. Cheap layout fix (drop `space-between` + minHeight, let header size to content).

**One decision reversal worth noting:** Psalm 91 level count. User's initial lean was 3 levels with "slightly more than 13 moves each"; my initial lean was 4 thematic. After seeing actual chunk counts (49 total), no 3-level split matched the natural thematic breaks cleanly — every 3-level option bled across at least one thematic cluster. User flipped to 4 thematic on the principle that thematic content helps memorization.

**Scoping-flow note.** Kept response-chunking discipline well — each decision in its own message with user confirmation before moving on. One dependency was flagged early (#3 level-select lock-state depends on #5 hybrid-progression) and resolved by locking #3 shell-only and returning for the lock-state visual after #5. Worked cleanly.

**Open for playtest / V-3b implementation:**
- Picker subtitle (listed alternatives).
- Locked-card visual styling (dimmed, exact opacity/filter TBD).
- Per-level target scores (defaults = `moves × 300`; adjust any level during playtest).
- Reference column width if any 2-digit-verse passage beyond Psalm 91 comes in with longer game names.

## Addendum — 2026-04-24 V-2 shipped (v1.1 → v1.2 → v1.3 post-playtest)

Session V-2 shipped in three passes within the same session:

- **v1.1 first-pass** — implementation followed the V-2 spec in
  DEFERRED.md (the canonical reference, written during the morning
  deep-scoping pass). Playtested by the user immediately after.
- **v1.2 post-playtest polish** — four fixes driven by the v1.1
  playtest, rolled into the same session since they're tight
  adjustments rather than new scope:
  1. **Header font revert.** v1.1 applied Georgia to the game-header
     `<h1>` for typographic consistency with the text bar; it fought
     the Arial header chrome. v1.2 drops the override — header stays
     Arial; Georgia is reserved for content typography (text bar +
     passage modal).
  2. **Reveal-on-decrement.** v1.1 fired the chunk reveal at post-
     settle + 200ms beat. Playtest finding: the reveal trailed the
     eye — the player was already scanning the cascade when the
     next chunk appeared. v1.2 fires the reveal the instant a
     successful swap decrements `moves`, so the next chunk is
     visible *during* the cascade. Drops `pendingRevealRef`, the
     settle-wait effect, and `CHUNK_REVEAL_BEAT_MS`; keeps the
     250ms CSS fade-in. Net code: simpler (one effect instead of
     two).
  3. **Text-bar tightening.** v1.1 set `minHeight: 96px` with
     `justifyContent: flex-end`, which left empty whitespace at the
     top of the container until 3 chunks were revealed, and the
     3-chunk max state was too tall to fit board + banner + text
     bar in a standard viewport. v1.2 drops min-height (container
     sizes to content), tightens padding (14px 20px → 8px 20px) and
     fonts (current 22 → 19px, prior 15 → 13px, line-height 1.3 →
     1.2). Trade-off: slight downshift of the board across the first
     two reveals (1 chunk → 2 → 3), then stable.
  4. **Start-of-round passage modal.** User ask: show the full
     passage at game start so the player sees the target before
     memorizing. New `showStartModal` state, init true; reuses the
     existing end-of-round passage modal with button-text and
     click-handler branching — `Begin game` at start (closes modal),
     `Play again` at end-of-round (calls `restartGame`). Per user
     request, `restartGame` also re-opens `showStartModal` so the
     passage is seen between rounds. Swap handlers pick up a
     matching `showStartModal || showPassageModal` guard as
     defense-in-depth alongside the existing `moves <= 0` guard.

- **v1.3 header trim** — after v1.2 playtest, the header + text bar
  + board still didn't fit in the user's browser viewport without
  scrolling (couldn't see both score at top and bottom-row tiles).
  Chose the chrome-cut path over shrinking tiles or removing rows
  (which would have changed gameplay dynamics). Changes:
  - Hide the "✨ Specials on board: N" line in VERSES_MODE
    (`!VERSES_MODE &&` guard on the div).
  - Hide the entire combo-indicator + idle-stats div in VERSES_MODE
    (min-height-24px slot goes away, not just collapse-when-idle).
    Score popups over the board still show points + multiplier
    per match, so mechanic feedback stays where the player's eye
    is already focused.
  - Header card padding `12px 20px → 10px 20px`, margin-bottom
    `20 → 14px`, minHeight `110 → 80px`, all in VERSES_MODE only.
  - Kept: title, Score, Moves, Bonus moves (when >0), Target,
    dark/light toggle.

Net build impact: verses bundle 64.37 → 65.56 kB (v1.0 → v1.3).
v1.3 came in ~1 kB smaller than v1.2 (66.59 → 65.56 kB) because
the guarded render branches are tree-shaken when VERSES_MODE is a
module-constant true — the build drops the combo-indicator JSX.
v1.2 had already been slightly smaller than v1.1 (66.78 → 66.59 kB)
thanks to the dropped settle-wait reveal pipeline.

The remaining implementation-note prose below still applies to v1.2
(wiring, versesLevel flattening, `restartGame` reset semantics,
gameState staying 'playing'). Two things changed from the v1.1
description:
- Chunk reveal is now one effect (moves-decrement fires reveal
  directly, no pendingRevealRef). End-of-round still waits for board
  quiet (isAnimating / combo / pendingSpecials) before its 2.5s
  modal hold.
- `restartGame` also sets `showStartModal = true` so the passage
  viewer re-appears between rounds.

**Implementation notes (not already in the spec):**

- **Settle-callback hook** (flagged in the V-2 spec as a commit-time
  follow-up): reveals are triggered by a pair of effects, not by
  splicing into the existing `setIsAnimating(false)` call sites.
  - A moves-decrement detector (`useEffect` on `moves` with a
    `prevMovesRef`) arms `pendingRevealRef` when moves decreases.
    Using moves as the trigger means only successful swaps (which
    consume a move) arm a reveal — non-match snap-backs don't, and
    `restartGame` resets the ref so the 0 → 12 bump isn't misread as
    a decrement.
  - A post-settle reveal effect on `[turnComplete, isAnimating,
    combo, pendingSpecials.length, gameState, moves]` fires a 200ms
    timer once the board is quiet, commits the reveal, and clears
    the pending flag. Non-reactive ref by design — this avoids
    touching the tablet cascade pipeline, which keeps the V-2 diff
    contained and preserves the pure-tablet v11.11 gameplay inside
    the match-3 loop.

- **Bonus-moves-prompt guard site** (also flagged in the spec):
  handled at the top of the existing arcade game-end effect's
  timeout body — a single `if (VERSES_MODE) return;` short-circuits
  before any of the three resolution branches (target-hit prompt,
  bonus-round resolution, moves=0 resolution). The earlier
  `pendingThreshold` bail and the separate bonus-move-accrual effect
  both stay live, so the 10k-threshold tracking keeps ticking —
  earned bonus moves sit idle at V-2 (no prompt consumes them) and
  get zeroed on Play Again. V-4 replaces the early-return with the
  memorize-mode victory-round + consume + arcade-handoff logic.

- **`gameState` never transitions in VERSES_MODE.** The arcade
  `won`/`gameover` transitions live inside the guarded flow above,
  so a VERSES_MODE game stays in `gameState === 'playing'` from
  first swap through passage-modal dismissal. Arcade end banner
  render is additionally wrapped with `!VERSES_MODE` (defensive —
  currently redundant).

- **Play Again** calls the existing `restartGame`. VERSES_MODE branch
  reseeds `moves` and `levelTarget` from `versesActiveLevel` (not
  the arcade random), resets `revealedChunkIndex` to 0 (chunk 0
  pre-visible again), clears toast + modal, zeros `pendingRevealRef`
  + `prevMovesRef`, and zeros `bankedMoves` (since V-2 has no
  consumption path; persisting would stockpile dead currency).

- **Build impact.** `dist/assets/verses-*.js` 64.37 kB → 66.76 kB
  (+2.39 kB). Verified Titus chunks are bundled into the chunk
  stream (`Titus 2:11`, `For the grace of God`, `Jesus Christ` all
  present in the emitted JS).

**Scoping-discipline check.** User's scope-adherence feedback rule
held up well — implementation stayed strictly inside the V-2 spec.
Items noticed but not touched: the pre-existing `console.log('Game
end check:', …)` at the top of the arcade game-end effect's timeout
body still fires every tick (now in VERSES_MODE too, since the log
is before the `if (VERSES_MODE) return`). Harmless console noise;
intentionally left for a future cleanup session rather than bundled
into V-2.

**Still-open playtest questions** (from V-2 scoping, now live):
- 200ms settle beat vs. 250ms fade — does the board settle "read"
  before the text shifts, or does the beat feel too long?
- 2.5s passage-modal hold — enough time to absorb the last chunk,
  or too long / too short?
- Target-hit toast duration (2.5s) + gold-✓ contrast on the
  header Target field.
- Georgia at the sizes chosen (22px current / 15px prior / 19px
  modal body) — legible and distinctive, or fighting the rest of
  the UI?

## Addendum — 2026-04-24 V-2 deep-scoping session

V-1 shipped as commit `d20830b` (pushed to `origin/main`). Immediately
after, ran a deep-scoping pass on V-2 across seven decision clusters
(Q1–Q7). V-2 originally described in the V-1-era DEFERRED entry as a
~4-bullet scope; that proved too thin to resume cold, so this session
pinned every open sub-decision. Outputs written back to
`docs/DEFERRED.md` V-2 entry (the canonical reference for V-2 coding)
and the core-mechanics section of the Memorize Mode block.

**Substantive design changes (not just detail fills):**
- **Titus chapter correction.** User's pasted chunk content was NKJV
  Titus **2**:11–13, not Titus 3:11–13 as the DEFERRED spec had said.
  Verified against NKJV; typo had ridden through the 2026-04-24 AM
  scoping session unnoticed. All references updated to Titus 2. Slug
  changed `titus-3-11-13` → `titus-2-11-13`.
- **First chunk pre-visible.** User ask: show the first chunk before
  the first match to ease memorization entry. Knock-on: `moves =
  chunks − 1` (was `moves = chunks`). For Titus, 12 moves. Default
  target formula unchanged — `moves × 300` — but the new move count
  yields a different default (3,600 instead of 3,900).
- **Target-hit notification at V-2.** User pushed back on the "silent
  at V-2, banner at V-4" plan: "It shouldn't be silent." Split the
  V-4 spec into two shippable pieces — toast + header state-change
  at V-2 now, 1.5× scoring mode + updated toast copy at V-4 later.
  Minimal V-4 churn (one string change + one scoring branch).
- **Typography pick.** Georgia serif for chunk content (user:
  "favorite font"). Carries through rolling text bar and passage-
  reveal modal for consistency. No ornaments — typography does the
  distinguishing work.

**Scoping discipline notes (behavioral feedback, also captured in
auto-memory):**
- User repeatedly hit the response-chunking rule. Q5 (end-of-round
  flow) was the main violation — shipped five sub-decisions stacked
  under one "topic" umbrella. User reply: "that's a lot of content
  to look through" + "I have to scroll up." Updated
  `feedback_response_chunking.md` with an explicit **~20–25 line
  cap per response**; scoping discussions should break sub-decisions
  into separate messages, not just topics.
- User flagged "glob", "slug", "kebab-case" as unfamiliar jargon.
  Added new `feedback_jargon.md` memory — define dev terms on first
  use or avoid.

**Follow-up for V-1 roster row:** V-1 commit hash `d20830b` filled
into the roster row as part of this doc commit (previously a
`(this commit)` placeholder).

## Addendum — 2026-04-24 Memorize Mode scoping session

Full scoping conversation for a new game mode — **Verses** (memorization
delivery via match-3). Fork from tablet v11.11. Core concept: verses
reveal one chunk per successful swap, riding on top of unchanged
match-3 mechanics. 5 sessions planned (V-1 through V-5). Key locked
decisions:

- **Memorization is purely additive.** No gameplay changes. Match-3
  runs exactly as tablet arcade; chunks reveal as a byproduct.
  Consistent with the "amplify fun, not interfere" corollary in
  DESIGN.md.
- **Moves = chunks.** Each passage sets its own move budget via the
  data (Titus 2:11–13 = 13 chunks = 13 moves).
- **Two-level structure for long passages.** Games can be split into
  sequential levels (e.g., Psalm 91 into 3–4 levels). Hybrid
  progression: strict first pass (must hit target to advance),
  free-replay for individual levels after full game completion. User
  flagged "middle parts are often the hardest" for memorization —
  free replay targets exactly that.
- **Secondary landing page** (Verses picker) on `verses.html`, with
  two-tier navigation (game cards → level-select for multi-level).
  Main `index.html` gets one "Verses" card (normal-visible, not
  admin-gated).
- **Silent 1.5× victory round** at target-hit. No prompt; "end early"
  would stop chunk reveals, which defeats the purpose. Keeps scoring
  reward while preserving memorization flow.
- **Bonus moves** carry level-to-level within a game, persist on
  "Play again" (to let players replay with accumulated rewards), zero
  on picker-exit, and roll into tablet arcade on "Tablet arcade"
  button. Pattern shares future plumbing with DEFERRED Session F's
  campaign → arcade carry — build standalone first, unify later.
- **Target score** = `moves × 300` formula default with optional
  per-level override. Short games may need the override since
  cascade setup is harder with few moves.
- **Stars** — campaign-style 1–5★ per level (tentative — removable
  later if they add noise). Chosen for replay motivation since hybrid
  progression specifically supports re-playing individual levels.
- **Text bar** between banner and board, rolling 3-chunk window
  (current + 2 prior), current emphasized. End-of-level passage
  reveal after ~2.5s delay. At final-game end, passage stays visible
  indefinitely with **Play again** / **Tablet arcade** buttons —
  no auto-dismiss.
- **Content authoring** — drop a new folder under
  `platforms/tablet-verses/games/<slug>/` with a `game.js` file;
  glob-discovery picks it up. `_template/` + README provided;
  `hidden: true` flag for drafts.
- **First games** — Titus 2:11–13 (single-level, 13 chunks) at V-2.
  Psalm 91 (multi-level, 3–4 levels TBD during V-3).

Scoping discipline: decisions worked through one-per-message over the
course of the conversation, with an explicit 7-item roadmap front-
loaded and confirmed before diving in. Reversals during scoping (worth
noting as sign the scoping caught real design questions):
- **Stars:** defaulted no → flipped to yes after user reasoned about
  replay motivation under hybrid progression.
- **Bonus moves on Play Again:** defaulted zero → flipped to persist
  after user framed them as an earned reward rather than per-session
  currency.

The full locked-decisions summary lives in `docs/DEFERRED.md` Memorize
Mode entry, which is the canonical reference for V-1 through V-5.

---

[Prior addenda from 2026-04-23 E-2a, E-1b, and scoping sessions are in
`docs/archive/PROGRESS-2026-04-23.md`.]
