# M3 — Progress & Session Roster

**Date:** 2026-04-23
**Purpose:** Living index of planned coding/dev sessions (A onward), what's
shipped vs. still to do, plus a narrative summary of key discussions and
decisions that aren't already captured in `DESIGN.md` or `DEFERRED.md`.

This doc is the "where are we and why" companion to those two. When a
decision is made during a session that turns into a design principle or
persistent TBD, it goes into DESIGN.md or DEFERRED.md; ephemera and
running status live here. Full historical detail for sessions through
2026-04-22 is in `docs/archive/PROGRESS-2026-04-22.md`.

---

## Session roster

Status key: **✅ shipped** · **🚧 in flight** · **📋 planned** · **🧊 deferred**

| Session | Scope | Status | Version shipped | Commit |
|---|---|---|---|---|
| A–L, J, J-port, B-2, H₁, H-2, E-1a | Earlier sessions shipped through 2026-04-22. See `docs/archive/PROGRESS-2026-04-22.md` for full per-session detail. | ✅ shipped | see archive | see archive |
| E-1a2 | Tablet-sim v1.0 → v1.1 — bot now plays until moves exhausted instead of stopping at target. The `while (score < levelTarget && …)` condition exited the loop as soon as score crossed target, so the bot never accumulated past target and never crossed the 10k bonus-move threshold in console testing. The "0 bonus moves in 50 games" finding from E-1a playtest was an artifact of this, not a real "bot can't reach it" signal. One-line fix; `won` flag still reflects target-reached, `finalScore` captures full play-through. | ✅ shipped | `match3-v1.1-tablet-sim.jsx` | (this commit) |
| E-1b | Simulation harness, 1-ply — admin UI wrapping `_SIM`. New Simulation Batch Runner card with bot dropdown, target + moves admin inputs (defaults 6000 / 20, sessionStorage-persisted), preset buttons (10 / 50 / 100 / 200 / 1000), async orchestration with Cancel, aggregate stats, vertical-bar score histogram with dashed target line + p10/median/p90 labels, collapsible per-game detail table, Copy JSON (Option C metadata + aggregates + raw). `_SIM.runGame({bot, target, moves, botParams})` final signature locked here so E-2a slots in without retrofit. Return shape adds `outcome`, `bonusMovesEarned`, `runtimeMs`. | ✅ shipped | `match3-v1.2-tablet-sim.jsx` | (this commit) |
| E-2a | Simulation harness, Monte Carlo bot + Web Worker pool + framework integration with E-1b. `_SIM` + shared constants extracted into new co-located `simCore.js`; imported by both main-thread v1.3 JSX and new `sim-worker.js` module worker. MC bot (`mcPickBestSwap`) with N rollouts per candidate, depth-cap-bounded playouts, 1-ply-heuristic or random rollout strategy; objective = expected total score at horizon. MC Parameters admin sub-section (N default 30, depth-cap default 5, rollout default 1-Ply, workers default `hardwareConcurrency−1`), all sessionStorage-persisted. Worker pool dispatched via Vite's `new Worker(new URL('./sim-worker.js', import.meta.url), { type: 'module' })`; dispatch-on-complete pattern; cancel force-resolves pending promise. JSON metadata adds `botParams` when MC; results display shows bot + key-params banner. | ✅ shipped | `match3-v1.3-tablet-sim.jsx` + `simCore.js` + `sim-worker.js` | (this commit) |
| E-2b | Simulation harness — MC tuning session. Run batches at varying N / depth-cap / rollout-strategy combos; lock defaults. Mostly runtime-config; v1.3 → v1.4 only if tuning changes source defaults. | 📋 planned | — | — |
| I | Reward-mode integration — after Session H values settle. Integrate reward-round trigger into main tablet arcade (every-5-wins → next round runs in reward mode, uses existing `currentRun`) and campaign (per-level `rewardMode: true` flag in `levels/campaignConfig.js`). Wire the progressive special-drop lever (level 5+, scaling up per 5 levels, cap at level 30). Announcement copy: first-time explanation modal at arcade level 5; reward-round-entry full-screen banner. | 📋 planned | — | — |
| F | Desktop time-attack grace window (parity with campaign v1.25) + post-campaign arcade continuation (tablet arcade with campaign bonus-moves carried forward) + animated tutorials (inline mini-grid demos on each campaign intro screen). | 📋 planned | — | — |
| G | Campaign → next version — port Session C's hypernova rework to the campaign file. Straightforward mechanical port; pending playtest of tablet v11.7 first to confirm the feel. Version TBD (campaign is v1.26 after Session L). | 📋 planned | — | — |
| D-2 | Step-mode platform — sibling platform at `platforms/tablet-stepmode/`, admin-only, pipeline refactor from callback-setTimeouts → `await`-based pauses, "Next step →" button. Explicitly separate platform rather than a version bump because the refactor carries regression risk for normal play. | 🧊 deferred | — | — |
| Research | Match-3 literature survey — multi-session dive into academic / industry research on match-3 AI, balance, level difficulty prediction, design patterns. Includes monetization/compulsion-loop research as foreign-perspective reference. To run on a day when tokens are fresh and nothing else is scheduled. Scoping in `DEFERRED.md`. | 📋 planned | — | — |

---

## Current platform status

| Platform | Active version | Notes |
|---|---|---|
| Tablet arcade | v11.11 | Session L contrast fix in admin panel + scoring-history drawer |
| Campaign tablet | v1.26 | Session L contrast fix in CampaignAdmin + shared AdminPanel import |
| Reward-mode sandbox | v1.1 | Session H-2 — all 5 levers wired; HUGE-turn popup live; dev-time lever-fire log toggle |
| Tablet Sim | v1.3 | Session E-2a — Monte Carlo bot + Web Worker pool. `_SIM` + constants extracted to `simCore.js` (shared with `sim-worker.js`). MC Parameters admin sub-section (N, depth-cap, rollout strategy, workers). Bot dropdown dispatches: 1-ply stays sync, MC uses worker pool |
| Desktop arcade | v12.1 | Needs bonus-moves update + timed-mode grace (Sessions F-ish) |
| Time Attack (desktop) | v12.1 | Needs grace window port from campaign v1.25 (Session F) |
| Phone 341 | v12.1 | Needs bonus-moves update (open since 2026-03-23 handoff) |
| Phone 418 | v12.4 | Session J-port: cap 99, run tracking, end-confirm, stacked bonus-moves banner |

---

## Rolling "what's next" suggestion

Updated after E-2a shipped (Monte Carlo bot + Web Worker pool) on
2026-04-23.

1. **Hands-on playtest of MC runs.** Run a small MC batch (e.g., 10 games at default N=30, depth=5, 1-ply rollouts) and confirm: worker spin-up works, live progress updates, cancel terminates cleanly, results banner shows the MC summary line, JSON metadata includes `botParams`. This is the first live test of the worker-pool code path and a prerequisite to E-2b.
2. **Device verification** of phone-418 v12.4 and tablet v11.11 on iPad (still open from 2026-04-22 — bonus-moves prompt, stacked banner, cap messaging, end-confirm popup, run tracking, contrast fix).
3. **E-2b — MC tuning session** — run batches at varying N / depth-cap / rollout-strategy / workers combos; compare MC outcomes against 1-ply baseline (target 6000, moves 20). Calibration success criteria in the E-2b starting brief below.
4. **Session I — reward-mode integration** — unblocked.
5. **Session G — campaign hypernova port** — mechanical duplication of Session C into campaign file.
6. **Session F bits** — desktop timeattack grace, post-campaign arcade continuation, animated tutorials.
7. **Research** — evening task with fresh tokens.

---

## Starting brief — next session: E-2b (MC tuning session)

**Version target:** `v1.3 → v1.4` only if tuning locks in via code changes
(e.g., changing defaults in source). No version bump if tuning is purely
runtime-config / sessionStorage adjustments.

**Scope:** run batches at varying N / depth-cap / rollout-strategy combos;
compare outcomes; lock sensible defaults. Calibration success criteria: MC
should show (at minimum) higher average score and/or meaningfully higher
win rate than 1-ply at comparable per-real-move cost. If MC crosses 10k
more often than 1-ply does post-v1.1, that's a clean directional signal.
Calibration is relative, not absolute — skilled humans are still above
both bots.

---

## Addendum — 2026-04-23 E-2a ship notes

Main structural decision locked during implementation: **extract `_SIM`
into a co-located shared module** rather than duplicating its body into
`sim-worker.js`. New file: `platforms/tablet-sim/simCore.js` (~500 lines),
imported by both the v1.3 JSX and the new Web Worker. simCore is edited
in place going forward (CLAUDE.md's never-overwrite rule applies to
versioned platform files, not shared supporting libraries — same
principle as `core/` top-level modules). The alternative — duplicate ~420
lines of _SIM inline in sim-worker.js — would have created a double-
maintenance surface for every future bot-logic edit; one-time refactor
cost paid to avoid that drift.

MC design decision: **objective = expected total score at horizon.** For
each candidate swap, run N rollouts each playing `depthCap` moves, pick
the swap with the highest average finalScore across rollouts. This is
consistent with v1.1's "play until moves exhausted" semantics — the
game's real metric is final score, so MC optimizes the same thing.
Alternative objectives considered:
- *Win probability at horizon* — misaligned, since the game doesn't stop
  at target and win/loss is a side effect of total score.
- *Score gained during rollout* — equivalent to expected total (common
  starting score across candidates), so picked expected total for
  directness.

Rollout-step budget simplification: each rollout step counts as 1 move
against `depthCap` regardless of whether it's a base move or an earned
bonus move. The rollout's purpose is short-horizon scoring estimation,
not full-game simulation, and flattening the distinction keeps total
rollout cost predictable at N × depthCap cascades per candidate swap.

Variance source: even with the deterministic 1-ply rollout strategy,
`fillEmpties` in each cascade uses `Math.random()` for the refill, so
different rollouts see different refill sequences and produce different
final scores. This is where rollout-to-rollout variance comes from;
N > 1 provides signal.

Cancel path: `Worker.terminate()` fires no event, so we track pending
workers in `workersRef` and expose an out-of-band `finish` function via
`mcFinishRef` that `cancel()` can invoke to force-resolve the MC batch
promise. Without this the run would hang indefinitely on cancel.

Build verification: sim bundle 90.43 kB (from 85.90 kB — +4.5 kB for MC
UI + worker orchestration). Separate worker chunk `sim-worker-*.js`
auto-emitted by Vite via `new Worker(new URL(...), { type: 'module' })`
pattern. Runtime verification (worker spin-up, cancel behavior, MC
metadata in JSON) is playtest — this is the first live test of the
worker code path.

Sessions that benefit from this landing:
- **E-2b (next)** — the tuning session this framework was designed for.
  Run MC at varying params; compare against 1-ply baseline. Success
  criteria: MC shows higher mean score and/or higher win rate than 1-ply
  at comparable per-move cost.
- **Future MC iterations** — any further bot work (MCTS, deeper
  horizons, learned rollouts) drops into simCore with zero worker-side
  plumbing changes.

---

## Addendum — 2026-04-23 E-1b ship notes

Post-fix rerun of the 1-ply console sim (100 games, v1.1) gave fresh
directional numbers that shaped E-1b's defaults:
- **Win rate 54%** against `_SIM`'s native variable targets (5000–6500
  range, avg ~5750) and variable moves (18–24, avg ~21).
- **Mean score 6,837** (pre-fix was ~4,840, +41%); range 1,060 → 26,690.
- **~21% of games crossed 10k** (earning ≥1 bonus move) — vs. 0% pre-fix
  (the artifact previously called out).
- **maxCombo range 2–10** (pre-fix max was 8); a 10-cascade chain on game
  96 drove a 26,690-score outlier.

Picked defaults for the admin UI: **target 6,000** (just above the mean,
clean round number, lands the fixed-target win rate in the ~45–55% zone
for a balanced histogram) and **moves 20** (matches live tablet arcade).
Both admin-tunable + sessionStorage-persisted.

Notable implementation-time decisions:
- Legacy `runGame` keys (`tileCount`, `levelTarget`) kept honored
  alongside the new `{bot, target, moves, botParams}` signature so
  prior `window.runSimGame()` calls from DevTools still work.
- `bonusMovesEarned` added as a new accumulator field in the return
  shape — `bonusMovesRemaining` is always 0 post-v1.1 because the bot
  consumes what it earns, so a separate "earned" counter was needed
  for the "% games earning ≥1" stat.
- Cancel during a running batch discards partial results (doesn't
  commit aggregates for the truncated run). Simplest model; can revisit
  if users want to keep the partial set.
- Index.html sim card desc + badge was still on `v1.0 · E-1a` —
  the v1.1 update was missed in the E-1a2 session. Rolled forward to
  `v1.2 · E-1b` in this commit.

Build: sim bundle 84.97 kB (up from 71.13 kB), ~14 kB for the batch-
runner UI + helpers. No errors, no new warnings.

---

## Addendum — 2026-04-23 scoping session: E-1b, E-2a, E-2b + E-1a2 fix

Long scoping conversation covering all three upcoming simulation sessions.
Key decisions captured in the starting briefs above. Highlights worth
persisting here:

- **"0 bonus moves in 50 games" was an artifact, not a signal.** The E-1a
  50-game console run showed zero games crossing 10k for a bonus move.
  User flagged mid-conversation that the bot had been told to stop at
  target score, so it never kept playing to accumulate past it. One-line
  fix (E-1a2) reversed the behavior. Rerunning with v1.1 will give real
  data; the "MC will cross 10k where 1-ply can't" motivation softens until
  we see fresh post-fix numbers.

- **Runtime math for MC.** User ran a 1000-game 1-ply batch in ~12s wall-
  clock. That gave us concrete per-move cost (~0.3ms) and realistic MC
  compute estimates: even at aggressive params MC is firmly Web-Worker +
  depth-cap territory. Sync execution off the table. Worker pool worth the
  ~75 lines of orchestration code for 4–8× speedup over single worker.

- **Target score — admin-adjustable not dynamic for sim.** User observed
  that `_SIM` in v1.0/v1.1 generates unrounded targets (e.g., 4837),
  whereas the live game rounds to 100. E-1b will pass an admin-set fixed
  target straight into `runGame`, bypassing `_SIM`'s internal generator
  for batch runs. A DEFERRED item added to tighten `_SIM`'s target
  generation to match live (round to 100) for any context where auto-
  generated targets are still used.

- **Depth-cap empirical range.** User wants depth-cap tunable specifically
  to test whether lookahead past ~10 moves matters — intuition is it
  shouldn't given match-3's state divergence after cascades and refills.
  E-2a defaults depth-cap=5 with tunable range 3–20 so E-2b can
  empirically check.

- **Framework designed to host both bots from E-1b onward.** `_SIM.runGame`
  API gets its final `{bot, target, botParams}` shape in E-1b (even though
  only one bot exists there) so E-2a doesn't retrofit. JSON metadata
  already includes a `bot` field. Stats schema stays unchanged across
  bots — distributions differ; metrics don't.
