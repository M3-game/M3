# M3 — Progress & Session Roster

**Date:** 2026-04-22
**Purpose:** Living index of planned coding/dev sessions (A onward), what's
shipped vs. still to do, plus a narrative summary of key discussions and
decisions that aren't already captured in `DESIGN.md` or `DEFERRED.md`.

This doc is the "where are we and why" companion to those two. When a
decision is made during a session that turns into a design principle or
persistent TBD, it goes into DESIGN.md or DEFERRED.md; ephemera and running
status live here.

---

## Session roster

Status key: **✅ shipped** · **🚧 in flight** · **📋 planned** · **🧊 deferred**

| Session | Scope | Status | Version shipped | Commit |
|---|---|---|---|---|
| A | Campaign v1.24 → v1.25 — post-playtest polish batch (transition pause 3s→2s, bonus-move cap 99, end-confirm popup, best-run tracking, timed-mode grace, backup/restore, admin access from level-select) | ✅ shipped | `match3-v1.25-campaign-tablet.jsx` | `f19626e` |
| B | Phone-418 deploy fix — `vite.config.js` was pinned to `v11`; v12.0/12.1/12.2 never actually shipped despite being committed. Bumped rollup input to v12.2 | ✅ shipped | `match3-v12.2-418px-phone.html` (now actually deployed) | `16efeb2` |
| C | Tablet v11.6 → v11.7 — hypernova rework: solo hypernova fires specials in footprint; all three hypernova combos (×2, ×supernova, ×bomb/cross/line) now fire specials in their zones + preserve distant specials + half-of-rest outside + 30-tile floor. Cascade stagger 400→480ms + match-transition 400→500ms on hypernova events. Threshold constants extracted. | ✅ shipped | `match3-v11.7-tablet.jsx` | `e04f259` |
| D | Tablet v11.7 → v11.8 — Playback / scoring-history panel (admin-only, left-side, game-wide accumulating, per-turn collapsible, click-to-flash tiles) + slow-motion playback (1× / 2× / 5× admin selector, wired through pipeline setTimeouts and cascade-delay helpers). | ✅ shipped | `match3-v11.8-tablet.jsx` | `3c71265` |
| D-2 | Step-mode platform — sibling platform at `platforms/tablet-stepmode/`, admin-only, pipeline refactor from callback-setTimeouts → `await`-based pauses, "Next step →" button. Explicitly **separate platform** rather than a version bump, because the refactor carries regression risk for normal play. | 🧊 deferred | — | — |
| J | Tablet v11.8 → v11.9 — Campaign parity + run tracking. (1) `BANKED_MOVES_CAP` bumped 25→99 + amber "Nearing cap" / red "Cap reached" header messaging. (2) "Are you sure?" confirm on in-header End-and-carry when below target. (3) Run tracking (consecutive won rounds): `currentRun` + `longestRun` persisted to localStorage, shown on end-game banner with 🏆 new-best badge. Terminology intentional: "run" not "streak" per user feedback. | ✅ shipped | `match3-v11.9-tablet.jsx` | `7497e72` |
| B-2 | Phone-418 blank-screen fix — v12.2 dropped one `)` while collapsing the in-header End-and-carry button's three string args into one string (line 2700). Parser reported error way downstream at 3070:10 (end of the style block template literal) but the real bug was upstream. One-char fix: `"))` → `")))`. | ✅ shipped | `match3-v12.3-418px-phone.html` | `718b846` |
| K | Tablet banner fix — hardcoded `v11.2` in the in-game banner span (line 3411) was stale from v11.2 ship-day; bumped to `v11.10` alongside a version file bump so the banner now tracks filename. No behavioral change. Flagged by user 2026-04-22. | ✅ shipped | `match3-v11.10-tablet.jsx` | `3acf59c` |
| L | Contrast fix — admin panel + scoring-history drawer text is low-contrast (off-white on dark) at small font sizes; hard to read. Lifted all text-color greys darker than `#ccc` to `#ccc` floor (per user call, even darker-than-AA-fail greys bumped); 10px uppercase labels → 11px + fontWeight 500; 11px helper text → 12px. Shared `core/AdminPanel.jsx` edited in place (new CLAUDE.md exception for shared core files). Tablet + campaign admin/history regions updated. | ✅ shipped | `match3-v11.11-tablet.jsx`, `match3-v1.26-campaign-tablet.jsx`, `core/AdminPanel.jsx` | (this commit) |
| J-port | Port Session J to phone-418 (tablet v11.9 → phone-418 v12.4). `BANKED_MOVES_CAP = 99` + amber / red cap messaging, run tracking (phone-418-scoped localStorage), "Are you sure?" confirm on in-header End-and-carry when below target, and bundled: banner restructured so "Bonus moves: N" is on its own row below Score/Moves/Target (was wrapping inline on 418px width). Header version label bumped v12.0-418px → v12.4-418px. | ✅ shipped | `match3-v12.4-418px-phone.html` | (this commit) |
| H₁ | Reward-mode sandbox — scaffold. New sibling platform `platforms/tablet-rewardmode/match3-v1.0-tablet-rewardmode.jsx` forked from tablet v11.11. New `rewardmode.html` entry + `vite.config.js` input + `src/entry-rewardmode.jsx`. Landing-page admin gate on `index.html`: long-press "🎮 M3" (1.5s) OR `?admin=1`, unlock persists in `sessionStorage['m3_admin_unlocked']`, reveals a hidden "Admin / Dev" section with the rewardmode card. `RewardmodeAdminWrapper` (forked from `TabletAdminWrapper`) adds a "Reward Mode Levers" card with 7 sliders (4 singles + 3 under "Special drop on big turn" sub-section). Values persist to sessionStorage `m3_rwd_*` keys; URL kebab-case params override sessionStorage on load. Non-default indicator (amber label + value + "(def N)" subtext) + "Reset to defaults" button. **Levers are not yet wired into game logic** — board plays like tablet v11.11. | ✅ shipped | `match3-v1.0-tablet-rewardmode.jsx` | (this commit) |
| H₂ | Reward-mode sandbox — wire the 5 levers into game logic: `TILE_TYPES_COUNT` at the random-tile-draw sites; `NEIGHBOR_MATCH_BIAS` + `CLUSTER_DROP_BIAS` in `applyGravity` refill; `CLUSTER_SEED_ON_START` after `initializeGrid`; `SPECIAL_DROP_ON_BIG_TURN` (threshold + super% + hyper%) on the cascade-end hook. Playtest + iterate lever ranges. | 📋 planned | — | — |
| I | Reward-mode integration — after H values lock in, integrate reward-round trigger into main tablet arcade (every-5-wins → next round runs in reward mode, uses existing `currentRun`) and campaign (per-level `rewardMode: true` flag in `levels/campaignConfig.js`). Wire the progressive special-drop lever (level 5+, scaling up per 5 levels, cap at level 30). Announcement copy: first-time explanation modal at arcade level 5; reward-round-entry full-screen banner. | 📋 planned | — | — |
| F | Desktop time-attack grace window (parity with campaign v1.25) + post-campaign arcade continuation (tablet arcade with campaign bonus-moves carried forward) + animated tutorials (inline mini-grid demos on each campaign intro screen). | 📋 planned | — | — |
| G | Campaign → next version — port Session C's hypernova rework to the campaign file. Straightforward mechanical port; pending playtest of tablet v11.7 first to confirm the feel lands before duplicating. Version number TBD (campaign is now v1.26 after Session L). | 📋 planned | — | — |
| E₁ | Simulation harness, 1-ply — in-browser admin button on tablet: run N simulated games with a 1-ply heuristic bot, using the existing game functions directly (no refactor). Output: win-rate per level, avg/min/max scores. **Skill-gap caveat:** 1-ply is lower-bound casual play; never tune level targets down from bot results alone. | 📋 deferred | — | — |
| E₂ | Simulation harness, Monte Carlo — new sibling platform at `platforms/tablet-sim/` that extracts game logic (grid init, match detection, gravity, cascade, scoring) into a pure framework-free module. **Only this sibling platform is refactored; main tablet file is never touched.** MC runs ~100 random playouts per candidate move, picks highest-average-score move. Calibrate by checking that sim score distributions land in the same range as observed human play, not by snapshot-identity matching. | 📋 deferred | — | — |
| Research | Match-3 literature survey — multi-session dive into academic / industry research on match-3 AI, balance, level difficulty prediction, design patterns. Includes monetization/compulsion-loop research as foreign-perspective reference. To run on a day when tokens are fresh and nothing else is scheduled. Scoping in `DEFERRED.md`. | 📋 planned | — | — |

---

## Narrative summary — what we've discussed and decided

Running record of substantive conversation threads whose conclusions aren't
directly reflected in another doc. This section is written for someone (or a
future session) picking up cold.

### iOS Safari storage purge — the real cause of "lost progress"

Early suspicion during Session A planning was that stats persistence was
broken. Turned out the localStorage keys are version-agnostic and don't reset
between versions. The real culprit is **iOS Safari's 7-day ITP storage
purge** — Safari auto-deletes localStorage for sites the user hasn't
interacted with for 7 days. Since the M3 site is hosted on GitHub Pages
(`m3-game.github.io`) and the user primarily plays on iPad, stats were
vanishing on a weekly cadence.

Fixes considered:

- **Manual export/import** (shipped in Session A for campaign): user can
  copy backup JSON to clipboard or download a file; paste back to restore.
  Low-friction safety net.
- **PWA install ("Add to Home Screen")** — reduces purge risk on iOS but
  doesn't eliminate it. Blocked on real icon assets.
- **IndexedDB migration** — some protection but iOS eventually purges it too.
- **Backend sync (Firebase etc.)** — full fix but adds ongoing maintenance
  + cost.

Decision: ship the manual export/import now; escalate only if the backup is
still being lost. PWA install is in DEFERRED.md pending icon assets.

### Phone-418 "bonus moves never prompt" was actually a deploy bug

User reported during Session C planning that phone-418 earns bonus moves
but never shows the prompt. Code audit concluded the logic was correct.
Cache-busting didn't help. The real issue: **`vite.config.js` had the
phone-418 rollup input pinned to `v11`** — so v12.0, v12.1, v12.2 all
committed in the source tree but never made it into `dist/` during build.
The user had been playing v11 the whole time. One-line fix (Session B).

Followup in DEFERRED.md: auto-copy all phone-418 versions into `dist/`
(via `public/` directory or `vite-plugin-static-copy`) so this class of
bug can't happen on future bumps.

### Tablet hypernova — audit found parity, user then wanted behavior change

Session C started as an audit task: "does the tablet hypernova match the
campaign's?" The audit concluded **yes, they're functionally identical**
(both solo and combo). The user realized their mental model ("solo hypernova
leaves half the board + all specials; combos should feel similar") diverged
from the existing code: specifically, hypernova combos were clearing all
non-specials (close to whole-board wipe), not "two footprints + half of
rest."

The user then made a **design decision** that the combo behavior should
change — and that solo hypernova should *also* fire specials caught in its
footprint (previously it skipped them). Session C implemented both changes.

This crystallized into a broader design principle — **"Variety through
amplification, not interference"** — which was added to `DESIGN.md`.
Skilled play involves setting up specials for future cascades; the game
shouldn't disrupt that planning by nuking everything.

### Skilled play is deeper than the simulation bots we'd initially planned

Originally scoped Session E as a simple 1-ply heuristic bot (pick best
immediate move by match length / special-creation). In discussion the user
clarified: skilled play is often 5–8 move lookahead — setting up a special
next to another special, aligning cascades across rows, queuing up a
mega-combo six moves out. A 1-ply bot captures none of that.

Consequence for Session E: treat 1-ply results as **lower-bound casual
play**, *not* as a calibration target. If the bot finds levels too hard,
that doesn't mean the levels *are* too hard. Monte Carlo (v3) or 2-ply
lookahead (v2) are closer to skilled play; human-replay capture is the
best ground-truth anchor.

This conversation also shaped the Reward-mode design: simulation should
happen *before* sandbox tuning so we can compare empirical difficulty-vs-
lever-setting curves, not guess.

### Commercial match-3 monetization mechanics are explicitly rejected

Ice, stone, jelly, locked tiles, and similar obstacles exist in most
commercial match-3 games because they extend time-to-engagement and
create monetization touch-points (boosters, extra moves for a fee,
time-skip payments). The user doesn't monetize; these mechanics reliably
make them put the game down. Captured in DESIGN.md as a narrow
exception-only policy for a hypothetical future long-form campaign
(<10% of levels, explicit marking, never default).

### "Match-3 literature" — there is no venerable tome

Clarification during a side discussion: I'd casually called published
match-3 research "literature" as if it were an established body. It isn't.
Scattered academic papers (FDG, AIIDE, IEEE CIG/COG, KDD) exist; King has
published some data-science work on Candy Crush level difficulty; Julian
Togelius and a few others touch match-3 in the broader game-AI space.
No coherent survey paper. Best "literature" is reverse-engineering shipped
games.

User parked a multi-session research task for when tokens are fresh —
three focused queries (difficulty prediction, bot architectures,
amplification-vs-interference design patterns), including monetization
research as foreign-perspective reference. See DEFERRED.md.

### "Specials fire in the footprint" was a genuine solo-hypernova change

During Session C planning the user said "I think hypernova should fire
specials in its footprint/zone, is that not what you're proposing?" —
but in fact the existing solo hypernova skipped specials entirely.
Changing solo hypernova to fire specials in its footprint (in addition
to changing the three combos to do the same) was a meaningful behavior
shift, not just a code cleanup. Both were shipped in Session C.

User's question and the clarification that followed illustrated a
useful pattern: the user's mental model of how the game *should* work
sometimes diverges from how it actually works today, and the audit is
what surfaces it. Design reviews are load-bearing.

### Session boundaries + commit discipline

Emergent pattern across sessions:

- Each session has a version bump on at most one platform file (A: campaign,
  C: tablet, D: tablet).
- Docs updates bundled with the session's code where possible, or shipped
  as a separate small commit right before.
- Pause points honored — "stop" / "pause" messages aren't questions, they're
  commands. State-on-disk summary at pause helps resume cleanly.
- End-of-session commits include detailed body text describing what
  changed and why; session number/scope in the subject line.
- TodoWrite used to track multi-step sessions; cleared between sessions.

### Session letters and naming

Sessions are labeled A, B, C, D, D-2, E, F, G, H, I, plus "Research."
Letters assigned roughly in the order the session was scoped, not
strictly by ship order. Session B's phone-418 deploy fix was a
drop-everything interruption that happened between A and C.

Future-me / future-sessions: if we add a new planned session, give it
the next available letter. If we realize an earlier session needs a
follow-up, suffix with a dash number (D → D-2) instead of renaming.

---

## Current platform status

| Platform | Active version | Notes |
|---|---|---|
| Tablet arcade | v11.11 | Session L contrast fix in admin panel + scoring-history drawer |
| Campaign tablet | v1.26 | Session L contrast fix in CampaignAdmin + shared AdminPanel import |
| Reward-mode sandbox | v1.0 | Session H₁ scaffold — sliders persist, levers not yet wired (H₂) |
| Desktop arcade | v12.1 | Needs bonus-moves update + timed-mode grace (Sessions F-ish) |
| Time Attack (desktop) | v12.1 | Needs grace window port from campaign v1.25 (Session F) |
| Phone 341 | v12.1 | Needs bonus-moves update (open since 2026-03-23 handoff) |
| Phone 418 | v12.4 | Session J-port: cap 99, run tracking, end-confirm, stacked bonus-moves banner. |

---

## Rolling "what's next" suggestion

Updated after Session K shipped (tablet banner fix) and after a
2026-04-22 scoping discussion on reward-mode ordering. Current
priority order:

1. **Device verification** of phone-418 v12.4 and tablet v11.11 on iPad — confirm React still mounts on both, bonus-moves prompt fires on win/fail, stacked bonus-moves banner renders correctly on phone, cap messaging appears at ≥90 (amber) and 99 (red), end-confirm popup fires below target, tablet banner now reads `v11.11`, admin panel + scoring-history drawer text readable.
2. **Playtest tablet v11.8 / v11.9 / v11.10 / v11.11** — scoring-history panel, slow-motion, hypernova combos, bonus cap 99, end-confirm popup, run tracking, contrast fix.
3. **Session H₂ — wire the reward-mode levers into game logic** (next code session). H₁ scaffold shipped (sliders + persistence + URL overrides). H₂ hooks the 5 levers into `initializeGrid` / `applyGravity` refill / cascade-end; then playtest.
4. **Session I — reward-mode integration** (5-win arcade trigger, campaign reward-level flag, progressive special-drop, announcement copy). Runs after H₂.
5. **Session G — campaign hypernova port** — mechanical duplication of Session C into the campaign file. Version target TBD (v1.27+ since v1.26 used for Session L contrast fix).
6. **Session F bits** — desktop timeattack grace, post-campaign arcade continuation, animated tutorials.
7. **Session E₁ — 1-ply simulation** (deferred; no priority).
8. **Session E₂ — Monte Carlo on sibling sim platform** (deferred; after E₁).
9. **Research** — evening task with fresh tokens.

Sessions E₁ and E₂ were deprioritized 2026-04-22 because pure-logic
refactor risk in the main tablet file outweighed the tuning-data
benefit. Reward-mode tuning will proceed by playtest feel, consistent
with how the project has been tuned to date.

All of the above are captured in `docs/DEFERRED.md` with full scoping.
This doc just tracks roster + status + narrative.

---

## Addendum — Session J-port shipped (phone-418 campaign parity + run tracking)

Port of tablet v11.9 features to phone-418, plus a small layout
restructure that bundled cleanly with the work.

**Port coverage** (all four pieces from tablet v11.9):
1. `BANKED_MOVES_CAP = 99` + `BANKED_MOVES_WARN = 90` constants; the
   bonus-move award path (in the scoring useEffect) now clamps with
   `Math.min(prev + newMoves, CAP)`. Header text turns amber at ≥WARN
   with "Nearing cap — max 99" subtext; turns red at CAP with "Cap
   reached — no additional moves can be earned". Moves earned past cap
   are silently dropped (flash still fires — matches tablet).
2. Run tracking with phone-418-scoped keys (`match3_phone418_currentRun`
   / `…_longestRun`) — deliberately separate from tablet's
   `match3_currentRun` / `match3_longestRun` so iPad and iPhone have
   independent runs. A useEffect on `gameState` increments on 'won' and
   persists both keys; 'gameover' preserves currentRun so the end-game
   banner can show the broken run. `restartGame` resets currentRun if
   the previous game was a loss.
3. `requestEndGameCarryBanked` wrapper on the in-header End-and-carry
   button: runs `calculateUnusedSpecialsBonus` against the same
   would-win logic `endGameCarryBanked` uses, so it only fires the
   confirm when the player is ACTUALLY below target (not just below
   before specials bonus). The confirm popup uses the same orange
   gradient as tablet but with tighter phone-sized padding.
4. End-game banner: run tracking display added between the Final Score
   line and the details column. Shows "Current run: N wins [🏆 new
   best!]" on win; "Run broken at N wins" on loss (or "No active run"
   if currentRun=0 at loss); always shows "· Longest run: N".

**Banner restructure** (user request, bundled):
The Score/Moves/Target row was a single flex row with `justify-content:
space-between`. The inline "Bonus moves: N" span inside the Moves div
was too wide for phone-418's 418px viewport and the browser was
soft-wrapping it so the number landed below "Moves: N". Fix: closed the
flex row after Target; "Bonus moves: N" (with cap messaging when
applicable) is now a separate row below the flex row, as a sibling
child of the outer HUD container. The bonusRoundActive banner and the
usingBankedMoves End-button also moved out of the flex row as siblings
— they render the same visually (they had `marginTop: '6px'`) but
they're now proper block-level rows instead of flex children. Net: one
`)` moved from the end of the bankedMoves button chain UP to just
after Target, with the new bonus-moves row inserted between.

Header title also bumped v12.0-418px → v12.4-418px (was stale; phone
equivalent of the tablet banner still showing v11.2).

**Verification**
- `node --check` on extracted v12.4 script: clean
- `npm run build`: clean; `dist/platforms/phone-418/match3-v12.4-*`
  present (128 kB, gzip 29 kB — up from v12.3's 120 kB / 27 kB; the
  delta is ~500 lines of added state/effects/UI).

---

## Addendum — Session B-2 shipped (phone-418 syntax fix)

Bisected cleanly: v11, v12.0, v12.1 all `node --check`-clean; v12.2 fails.
Diffing v12.1 vs v12.2 surfaced the bug at the in-header End-and-carry
button (phone-418 line 2700). v12.2 simplified the button's visible text
from three `React.createElement` args (`"End and carry "`, emoji,
`bankedMoves`, `" moves forward"`) down to one (`"End and carry moves
forward"`) but also trimmed one closing paren from the chain: was
`"))), /*...*/React.createElement("div"`, became `")), /*...*/...`.
That dropped the outer grouping close, so every subsequent `)` in the
file appeared one short. Parser didn't surface the complaint until the
final template literal at line 3070:10 (end of the style element), which
is why the inspector pointed there.

Fix in v12.3: restore one `)` → `"))), /*...*/React.createElement("div"`.
One-character change, comment block updated. The other "End and carry
moves forward" instance on line 2879 (the bonus-prompt version) already
had `)))` and was untouched.

Deployment state: `vite.config.js` now points at v12.3; `index.html` card
updated; `npm run build` clean; v12.3 visible in `dist/` (120 kB, gzip
26.8 kB). User to verify on device before push.

This resolves the phone-418 "bonus moves earned but prompt never fires"
report (2026-04-20) — root cause was React never mounting, so the v12.2
bonus-moves logic (which does fire on win AND fail per campaign parity)
never had a chance to run.

---

## Addendum — Session J shipped (later the same day)

Went back to tablet after Session D to bring the v11.8 file up to
campaign-v1.25 parity plus a tablet-specific run metric:

- Bonus-move cap bumped 25 → 99 with amber/red "nearing / at cap"
  header messaging mirroring campaign v1.25 exactly.
- "Are you sure?" popup on the in-header End-and-carry button when
  below target during active bonus-moves use — mirrors
  `requestEndLevelCarryBanked` pattern from campaign.
- Run tracking. `currentRun` = consecutive won rounds since last loss.
  `longestRun` = all-time best. Incremented in a gameState-watching
  effect. Reset deferred to `restartGame` so the end-game banner can
  show "Run broken at N wins." 🏆 new-best badge when currentRun beats
  longestRun.
- User terminology preference memorialized: **"run" not "streak"** in
  tablet arcade (saved as persistent feedback memory).

---

## Addendum — Session L shipped (contrast fix)

Small session; two decisions worth capturing.

**Shared `core/` files exempt from never-overwrite rule.** The CLAUDE.md
versioning convention was written for platform files (each edit forces
archive + new versioned file). A shared file like `core/AdminPanel.jsx`
is imported by every platform that uses an admin UI; versioning it
would cascade renames across all importers for any small edit. The
user's call: shared `core/` files are edited in place, no archive, no
rename. Platform files still follow the versioning rule. CLAUDE.md
updated in the same commit to capture this.

**`#ccc` floor for text colors.** Initial plan was to fix only
WCAG-AA-failing greys (`#444`–`#777`) to `#aaa`. User's preference on
review was stronger: any grey darker than `#ccc` (including `#888`,
`#aaa`, `#bbb`) becomes `#ccc`. "`#888` is too dark — don't use it."
Reason: prioritize visibility over mathematical AA-pass. Small-text
bumps also applied per DEFERRED guidance: 10px uppercase labels →
11px + fontWeight 500; 11px helper text → 12px.

**Scope.** `core/AdminPanel.jsx` (8 edits, in place); tablet v11.11
(ScoringHistoryPanel + TabletAdminWrapper, ~10 edits); campaign v1.26
(inline CampaignAdmin, 5 edits). Desktop, phones do not import the
admin panel and were not touched. Borders/backgrounds unchanged —
contrast rule applies only to text `color`.

**Verification.** `npm run build` clean. Dev server started for on-
screen check before commit.

---

## Addendum — Session H₁ shipped (reward-mode sandbox scaffold)

Split decision made during this session (based on 5-hour budget concern):
Session H originally scoped as a single 3–4 hr session covering both
scaffold and lever wiring. Split into **H₁ scaffold** (~2 hrs shipped
here) and **H₂ lever wiring** (future). H₁ delivers a fully-working
admin UI with sliders + persistence, but the levers are cosmetic until
H₂ hooks them into game logic.

**Scaffold structure:**
- `platforms/tablet-rewardmode/match3-v1.0-tablet-rewardmode.jsx`
  (forked from tablet v11.11 — inherits contrast fix, run tracking,
  bonus-cap 99, end-confirm, scoring-history panel).
- `src/entry-rewardmode.jsx`, `rewardmode.html`, new `rewardmode`
  rollup input in `vite.config.js`.
- Landing-page admin gate in `index.html`: inline `<script>` detects
  long-press on `🎮 M3` title (1500ms, 10px move tolerance) and
  `?admin=1` URL param; persists unlock in
  `sessionStorage['m3_admin_unlocked']`. CSS class `body.admin-
  unlocked` reveals a hidden `<section class="admin-section">` below
  the main grid. Future admin platforms (step-mode, sim) drop into
  the same section.
- New `badge-admin` CSS class (blue variant) distinct from stable /
  WIP badges.

**Admin UI:**
- `RewardmodeAdminWrapper` (forked `TabletAdminWrapper`): title
  "🔧 ADMIN — Reward Mode Sandbox".
- New "Reward Mode Levers" card with 7 sliders:
  - `tile_count` (3–6, default 6)
  - `neighbor_bias` (0–50%, default 0)
  - `cluster_seed` (0–4, default 0)
  - `cluster_drop_bias` (0–50%, default 0)
  - `big_turn_threshold` / `big_turn_super_pct` / `big_turn_hyper_pct`
    grouped under "Special drop on big turn" sub-section
- Non-default indicator: label + value turn amber (#ffd54f) with
  "(def N)" subtext. Card header shows "● modified" when any lever
  is off-default.
- "Reset to defaults" button: disabled at defaults, enabled (red
  text) otherwise. Clicking clears all 7 sessionStorage keys + resets
  state (no page reload needed).
- Preserved from tablet: Playback card (scoring history toggle,
  speed buttons, clear history) and "📊 Open game stats →" launcher.

**Persistence:**
- Load order: URL kebab-case param → sessionStorage (`m3_rwd_*`
  keys) → default. URL params override sessionStorage on page load
  only (not rewritten live as sliders change).
- URL examples: `?tile-count=4&neighbor-bias=30&cluster-seed=2`.

**Verification.** `npm run build` clean (rewardmode bundle 67.97 kB,
+3.6 kB over tablet fork for the new card + state + helpers). Dev
server tested on-screen by user: landing-page gate, slider render,
persistence, URL override, reset button, non-default indicator — all
as expected.
