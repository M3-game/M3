# M3 — Progress & Session Roster

**Date:** 2026-04-27
**Purpose:** Living index of planned coding/dev sessions, what's
shipped vs. still to do, plus a narrative summary of key discussions
and decisions that aren't already captured in `DESIGN.md` or
`DEFERRED.md`.

This doc is the "where are we and why" companion to those two. When a
decision is made during a session that turns into a design principle
or persistent TBD, it goes into DESIGN.md or DEFERRED.md; ephemera and
running status live here. Detail for sessions through 2026-04-25 (Matt 5
content track) is in `docs/archive/PROGRESS-2026-04-25.md`. V-* track
narrative through 2026-04-24 is in `docs/archive/PROGRESS-2026-04-24.md`.
Earlier in `docs/archive/PROGRESS-2026-04-23.md`.

---

## Session roster

Status key: **✅ shipped** · **🚧 in flight** · **📋 planned** · **🧊 deferred**

| Session | Scope | Status | Version shipped | Commit |
|---|---|---|---|---|
| A–L, J, J-port, B-2, H-1, H-2, E-1a, E-1a2, E-1b, E-2a | Earlier sessions through 2026-04-23. See `docs/archive/PROGRESS-2026-04-23.md`. | ✅ shipped | see archive | see archive |
| V-1, V-2, V-3a, V-3b, V-4 | Verses platform — scaffold through stars / persistence / arcade-handoff / 1.5× victory round / "see entire passage" / VERSES_MODE arcade-banner suppression. Tablet-verses ended 2026-04-24 at v1.7; tablet arcade at v11.12. Full per-session detail in `docs/archive/PROGRESS-2026-04-24.md`. | ✅ shipped | v1.0 → v1.7 | b1773c7 (last) |
| Matt 5 v1.0 content | Verses content — Matthew 5 (partial). 5:1–20 across three NKJV levels. | ✅ shipped | (content) | aa730dd |
| Matt 5 sections 4 + 5 content | Verses content — Matt 5:21–26 + 5:27–30 appended to the existing game file. Brings Matt 5 to 5:1–30 across 5 levels. | ✅ shipped | (content) | d798e1c |
| **M-1** | **Phone-418 → JSX migration.** Format conversion HTML → JSX, zero behavior change. Created [phone418.html](../phone418.html) + [src/entry-phone418.jsx](../src/entry-phone418.jsx); converted [platforms/phone-418/match3-v13.0-418px-phone.jsx](../platforms/phone-418/match3-v13.0-418px-phone.jsx) from v12.4 (archived); updated [vite.config.js](../vite.config.js) and [index.html](../index.html). Build clean (50.52 kB bundle, gzipped 15.32 kB). Tablet behavior-parity comparison appended below as addendum. | ✅ shipped | v13.0 | (this commit) |
| **M-2** | **Phone-418 v11.7 hypernova rework port.** Backport tablet's "amplification, not interference" hypernova logic (solo + combos fire specials in footprint, preserve specials outside, half-of-rest with 30-tile floor). Discovered as gap during M-1 parity comparison. | 🚧 in flight | — | — |
| **T-1** | **Banked → bonus terminology refactor.** Cross-platform rename across 5 active platform files + `core/AdminPanel.jsx` + DESIGN.md + DEFERRED.md. Includes localStorage `BANKED_KEY` migration strategy. | 📋 planned | — | — |
| Verses content — Matt 5:31–48 + Isaiah 52:13–53:12 ESV | Pending user-edited drafts (since 2026-04-26). Matt 5:31–48 NKJV (3 sections) appends to existing game file; Isaiah 52:13–53:12 ESV is a new game file at `platforms/tablet-verses/games/isaiah-52-53/game.js` (first non-NKJV game). | 📋 planned | — | — |
| E-2b | Simulation harness — MC tuning. Deferred 2026-04-23; not blocking. | 🧊 deferred | — | — |
| V-5 | Verses admin + polish. Deferred 2026-04-24, "if needed." | 🧊 deferred (if needed) | — | — |
| I | Reward-mode integration — after Session H values settle. | 📋 planned | — | — |
| F | Desktop time-attack grace + post-campaign arcade continuation + animated tutorials. | 📋 planned | — | — |
| G | Campaign hypernova port. | 📋 planned | — | — |
| D-2 | Step-mode platform. | 🧊 deferred | — | — |
| Research | Match-3 literature survey. | 📋 planned | — | — |

---

## Current platform status

| Platform | Active version | Notes |
|---|---|---|
| Tablet arcade | v11.12 | Reads `m3_arcade_carry_from_verses` localStorage receipt at mount, shows 2.5s "+N bonus moves carried from memorize mode" top banner, clears the key. |
| Campaign tablet | v1.26 | Session L contrast fix in CampaignAdmin + shared AdminPanel import. |
| Reward-mode sandbox | v1.1 | Session H-2 — all 5 levers wired; HUGE-turn popup live; dev-time lever-fire log toggle. |
| Tablet Sim | v1.3 | Session E-2a — Monte Carlo bot + Web Worker pool. |
| Tablet Verses | v1.7 | Engine unchanged 2026-04-25. **Content as of 2026-04-25:** Titus 2:11–13 (single-level, 13 chunks), Psalm 91 (4 levels, 49 chunks), Matthew 5 partial (5 of ~9 sections, 90 chunks across 5:1–30). |
| Desktop arcade | v12.1 | Needs bonus-moves update + timed-mode grace (Sessions F-ish). |
| Time Attack (desktop) | v12.1 | Needs grace window port from campaign v1.25 (Session F). |
| Phone 341 | v12.1 | Needs bonus-moves update (open since 2026-03-23 handoff). |
| **Phone 418** | **v13.0** | **Session M-1 (2026-04-27): JSX migration.** Was a single standalone HTML file with CDN React; now a Vite JSX module mirroring phone-341's pattern. Zero behavior change vs v12.4. v12.4 stays in `archive/`. **Sync gap with tablet:** v11.7 hypernova rework not ported (M-2 will fix); also missing AdminPanel/`recordGameResult()` integration and verses → arcade carry banner. |

---

## Rolling "what's next" suggestion

Updated after M-1 ship 2026-04-27. Phone-418 catch-up + verses
authoring are the active tracks.

1. **M-2 — Phone-418 v11.7 hypernova rework port.** In flight after M-1
   commit. Single function rewrite (`activateSpecialTile` case
   'hypernova') + the hypernova combo branches in
   `activateSpecialCombination`. Real gameplay impact for phone — solo
   hypernova currently never fires specials, combos clear all
   non-specials. Tablet has had the rework since v11.7. Smallest
   scope, biggest gameplay impact among M-1's surfaced gaps.
2. **Verses content — Matt 5:31–48 + Isaiah 52:13–53:12 ESV.** User
   has edited drafts ready since 2026-04-26. Matt 5:31–48 appends
   three sections to the existing game file; Isaiah is a new file
   (first non-NKJV game). Workflow: I draft, user edited externally;
   user pastes edits at session start.
3. **T-1 — Banked → bonus terminology refactor.** Cross-platform
   rename across 5 active platform files + core + docs. Coordinate
   localStorage `BANKED_KEY` migration (rename with backward-compat
   read of old key).
4. **Phone-418 follow-on — AdminPanel + `recordGameResult()` port.**
   Brings phone-418 in line with tablet v11.2 admin/stats foundation.
   Lower priority than M-2 (no gameplay impact).
5. **Phone-418-verses (post-M-1, post-Matt 5/Isaiah).** Build phone
   form factor of the verses platform. Now feasible because phone-418
   is JSX. Includes verses → phone-418 arcade carry banner wiring.
6. **Reward-mode playtest iteration** — ongoing user-driven tuning.
7. **Device verification** of phone-418 v13.0 + phone-418 v12.4 on
   actual iPhone (regression check vs. archived HTML).
8. **Session I — reward-mode integration** — unblocked once
   reward-mode lever values settle.
9. **Session G — campaign hypernova port** — analogous to M-2 but
   for campaign.
10. **Session F bits** — desktop time-attack grace, post-campaign
    arcade continuation, animated tutorials.
11. **Research** — evening task with fresh tokens.

---

## Addendum — 2026-04-27 M-1 phone-418 JSX migration shipped

Format-only conversion. Phone-418 was the lone holdout from the repo's
JSX/Vite norm — every other platform (tablet, desktop, campaign,
phone-341, tablet-verses, tablet-rewardmode, tablet-sim) is a Vite JSX
module under `src/entry-*.jsx` + `*.html` entry pages; phone-418 was a
single standalone HTML file with CDN React script tags and
`React.createElement(...)` chains. Architecture.md's intended structure
acknowledges no exceptions; phone-418's status as exception was
undocumented except in HANDOFF-2026-03-23v12. Migration brings it in
line so phone-418-verses can be a JSX peer and the toolchain is
unified across the repo.

**Scoping decisions (locked at session start):**

1. **Format conversion only — zero behavior changes.** Sets the gate.
2. **Version number v13.0** (not v12.5) — format change is significant
   enough for a minor bump per CLAUDE.md.
3. **Idiomatic JSX conversion** (not "minimal" preserving
   `React.createElement(...)` chains). Reasoning: every other platform
   uses idiomatic JSX, and the tablet behavior-parity comparison is
   only meaningful with JSX-to-JSX diffs.
4. **CSS handling — match repo norm.** No `.css` file (the repo has
   zero of them). Per-element styling stays as inline `style={{}}`.
   Page-level CSS reset + body/`#root` styling + `@keyframes` all
   live in a single `<style>` element rendered at the bottom of the
   component, matching tablet, phone-341, tablet-verses. iOS
   browser-fallback CSS (`min-height: -webkit-fill-available`,
   `-webkit-tap-highlight-color`) preserved.
5. **Test plan:** build verification + manual playthrough on iPhone
   (or 418px Chrome devtools) + deep tablet behavior-parity comparison.

**What shipped:**
- Created [phone418.html](../phone418.html) (mirrors
  [phone341.html](../phone341.html)).
- Created [src/entry-phone418.jsx](../src/entry-phone418.jsx)
  (mirrors [src/entry-phone341.jsx](../src/entry-phone341.jsx)).
- Created [platforms/phone-418/match3-v13.0-418px-phone.jsx](../platforms/phone-418/match3-v13.0-418px-phone.jsx)
  — converted from v12.4 HTML; idiomatic JSX; npm React imports;
  inline `style={{}}` for per-element styling; bottom `<style>` block
  for page-level CSS + 4 `@keyframes`; `export default Match3Game`.
- Updated [vite.config.js](../vite.config.js) — replaced direct HTML
  reference with `phone418.html` rollup input.
- Updated [index.html](../index.html) line 104 link from raw HTML
  path to `phone418.html`.
- Archived `match3-v12.4-418px-phone.html` to
  `platforms/phone-418/archive/`.

**Build verification:** `npm run build` clean, 63 modules transformed
in 901ms, zero warnings. `phone418-*.js` bundle 50.52 kB (gzipped
15.32 kB) — comparable to phone-341 (45.50 kB), as expected for a
peer phone platform. Bundle sizes for other platforms unchanged,
confirming nothing else was disturbed.

**"Banked" naming preserved verbatim** — Session T-1 will rename
across all platforms in one disciplined pass. Renaming during the
migration would have muddied the regression check (was the breakage
JSX conversion or rename?) and left phone-418 inconsistent with the
rest.

---

## Addendum — 2026-04-27 Phone-418 v13.0 vs Tablet v11.12 parity comparison

Run as part of M-1's test plan. Detailed below.

**Identical (gameplay constants).** All scoring rules, multipliers,
thresholds, and rule constants match exactly: ROWS/COLS,
MIN_MOVES/MAX_MOVES, BASE_TARGET/TARGET_VARIANCE, WIN_BONUS_PER_MOVE,
EARLY_END_BONUS_PER_MOVE, BONUS_ROUND_MULTIPLIER (1.5),
DIFFICULTY_INCREMENT_MIN/MAX, BONUS_MOVE_INTERVAL (10000),
BANKED_MOVES_CAP (99), BANKED_MOVES_WARN (90), ANIMATION_SPEED,
MAX_DPR, FRAME_SKIP. Banked-moves logic, bonus round, run tracking,
end-confirm, special-tile core rebalance — all match tablet v11.9-era
logic.

**Intentional differences (form factor / storage scoping).**
TILE_SIZE: tablet 50, phone 40. TILE_GAP: tablet 4, phone 2 (smaller
tiles + tighter gap for narrower screen). Banked-moves localStorage
key: tablet uses shared `BANKED_KEY` from `core/AdminPanel.jsx`;
phone uses phone-scoped `match3_phone418_bankedMoves`. Run-tracking
keys similarly phone-scoped. Per a 2026-03-23 design decision so
iPad and iPhone have distinct runs.

**Real gaps — sync drift, not migration regressions.** Phone-418 v12.4
(J-port) brought it to parity with tablet **v11.9**. Tablet has shipped
v11.10–v11.12 since, plus v11.7 (Session C) and v11.8 (Session D)
that never landed on phone. The migration to v13.0 preserved the v12.4
baseline verbatim, so these gaps are inherited:

1. **v11.7 hypernova rework — "amplification, not interference."**
   Most significant gap. Tablet's hypernova fires specials in the
   5×5+row+col footprint, preserves specials outside, clears half of
   non-specials outside (30-tile floor). Phone-418 v13.0 still has
   v10.1-era logic: solo hypernova never touches specials; combos
   like `hypernova+hypernova` clear ALL non-specials. **Real
   gameplay divergence.** Hypernova feels different on phone than
   tablet. M-2 will port.
2. **AdminPanel integration.** Tablet imports `AdminPanel,
   defaultStats, STATS_KEY, BANKED_KEY` from `core/AdminPanel.jsx`.
   Phone-418 has none — no `?admin=1` URL access, no long-press-score
   panel, no scoring history.
3. **`recordGameResult()` stats system.** Tablet writes unified
   `match3_stats` JSON to localStorage on every game end. Phone-418
   writes only separate `match3_highScore`/`match3_highCombo`/
   `match3_highTurnScore` keys.
4. **v11.8 scoring history panel + slow-motion playback.** Admin-only,
   tablet only. Acceptable to skip on phone form factor.
5. **v11.12 verses → arcade carry banner.** Tablet reads
   `m3_arcade_carry_from_verses` on mount. Phone-418 has none — but
   no phone-418-verses exists yet, so unused. Wire when phone-418-verses
   ships.
6. **`HYPERNOVA_MIN_TILES_CLEARED = 30` constant extraction.**
   Cosmetic — phone hardcodes `30` inline, tablet has it as a named
   constant. No behavior difference.

**Migration verdict.** Clean. v13.0 preserves v12.4 verbatim, just in
JSX form. Build clean, bundle size in expected range. Behavior risk:
low (no logic changes). Sync gap with tablet: real but pre-existing;
the migration didn't widen or narrow it.

---

[Full V-* track narrative + 2026-04-25 Matt 5 detail are in
`docs/archive/PROGRESS-2026-04-25.md`. V-* per-session in
`docs/archive/PROGRESS-2026-04-24.md`. Earlier in
`docs/archive/PROGRESS-2026-04-23.md`.]
