# M3 — Progress & Session Roster

**Date:** 2026-08-22
**Purpose:** Living index of planned coding/dev sessions, what's
shipped vs. still to do, plus a narrative summary of key discussions
and decisions that aren't already captured in `DESIGN.md`,
`DEFERRED.md`, `docs/verses/TODO.md`, or `docs/verses/game-design-notes-v2.md`.

> **Personal-doc note:** `docs/claude-code-quickstart.md` is a personal
> reference doc the user maintains about working with Claude Code through
> Cursor's IDE. Not project documentation; future sessions can ignore it.

> **Naming note (Session P-2, 2026-05-02):** The two phone platforms were
> renamed: `phone-418` → `phone` (arcade), `phone-418-verses` →
> `phone-verses`. Phone-341 was retired. Earlier rows/prose use the old
> names where they describe events from that era.

> **Doc structure note (2026-05-25):** Verses docs live under
> `docs/verses/`. Verses-specific planned items live in
> `docs/verses/TODO.md`. DEFERRED.md remains the project-wide deferred
> roster + "Done" archive.

Per-day archived progress docs:
- 2026-08-09 (Sessions 15–19: phone-verses header clipping, vertical layout,
  level-select overlap, deploy retry, phone arcade tutorial + header parity):
  `docs/archive/PROGRESS-2026-08-09.md`
- 2026-07-04 (tutorial ports to tablet-verses + phone-verses; verses bug
  fix; bonus moves in verses; arcade-on-free-replay; "See passage";
  "Go to Verses" on both arcades; sandbox port; title-nudge tweak):
  `docs/archive/PROGRESS-2026-07-04.md`
- 2026-07-03, 06-27, 06-23, 06-22, 06-20, 05-25, and earlier — see
  `docs/archive/`.

---

## Session roster

Status key: **✅ shipped** · **🚧 in flight** · **📋 planned** · **🧊 deferred**

| Session | Scope | Status | Version shipped | Commit |
|---|---|---|---|---|
| (all sessions through 2026-08-09, Sessions 1–19) | See `docs/archive/PROGRESS-2026-08-09.md` and earlier archives. | ✅ shipped | see archive | see archive |
| **Escalating target persistence — Session 20 (2026-08-22)** | **Player-reported: going from tablet-verses to tablet arcade reset the raised target back to base. Cause — `difficultyBonus` was in-memory React state, discarded by any page load, while the run counter was already persisted. Three changes: persist the accumulation (including the first-round initializer, not just `restartGame`); replace the flat 200–500 increment with four tiers stepping at 12/24/36 wins, routed through one shared helper; add a "Highest target reached" stat on the end screen plus a 5-second in-game marker. Also scoped and recorded a full set of reward-level decisions for Session I.** | **✅ shipped** | **tablet arcade v12.1** | **(this commit)** |
| **Bonus-moves cap divergence — Session 21 (2026-08-22)** | **Player-reported: arriving in the reward-mode sandbox with 100+ bonus moves and playing knocked the total to 99, and the reduced number followed back into arcade and verses. Cause — the tablet-family platforms share one stored count (`match3_bonusMoves`), but the reward-mode sandbox and Tablet Sim were still on the pre-rename constant name `BANKED_MOVES_CAP` and so were skipped by the tablet v11.17 bump from 99 → 999. Fixed in both (cap → 999, warn → 900). Structural fix — moving the constant into `core/` — approved in principle and deferred.** | **✅ shipped** | **rewardmode v1.5 · tablet-sim v1.6** | **(this commit)** |
| **Docs: architecture refresh + multi-player direction — Session 22 (2026-08-22)** | **No code. Recorded the user's goal of multiple players across phone and tablet with optional linking, as direction rather than scheduled work. `docs/Architecture.md` was five months stale and not read at session start, so it was archived and rewritten as `docs/Architecture-2026-08-22.md`, and CLAUDE.md now lists it as required session-start reading alongside PROGRESS and DEFERRED. DEFERRED gained a "Multi-player and device linking" section with the four stages; DESIGN gained the player-visible fact that bonus-move pools are per device family. Also corrected a stale storage-key name introduced in Session 21. CLAUDE.md gained a rule against invented words.** | **✅ shipped** | **(docs only)** | **(this commit)** |
| **Menu declutter + builder reference — Session 23 (2026-08-22)** | **No gameplay code. The ten game-select cards carried up to 2,300 characters of accumulated version history each, burying the version number behind a wall of text; each now shows one line (version + latest change) and the full history moved to `docs/version-history-2026-08-22.md`, which also absorbs the archived `version-urls` doc. New `docs/builder-reference-2026-08-22.md` — an operator's manual for the user rather than for a coding session: sharing links, collecting a tester's exported stats, checking what's live, running locally, and which storage key holds what. CLAUDE.md gained a rule to keep it accurate. Also fixed the stale "99 cap" line in DESIGN.md.** | **✅ shipped** | **(docs + menu page)** | **(this commit)** |
| **Phone escalating-target port — Session 24 (2026-08-22)** | **Same bug as tablet v12.1, on phone arcade: `difficultyBonus` was in-memory only, so the phone-verses round trip (and any refresh) reset the target. Port of all three v12.1 changes — persistence under a PHONE-SCOPED key, the 12/24/36 tiers via one shared helper, and "Highest target reached" on the end screen. The 5-second marker floats above the target here rather than sitting inline, because the phone stat row is too narrow. Also recorded that phone arcade's difficulty constants are inherited from tablet and unvalidated on its smaller board.** | **✅ shipped** | **phone arcade v14.1** | **(this commit)** |
| **Terminology + task order — Session 25 (2026-08-22)** | **No platform code. Named the three select screens — game select (`index.html`), passage select, level select — after finding the live docs using five different names for the first one. `index.html` retitled from "Version Select" to "Game Select" (the cards are platforms and modes, not versions). All three added to the DESIGN.md terminology table. Recorded the agreed task order in DEFERRED, with the reasoning for putting core storage before reward mode. The `VersesPicker` → `PassageSelect` rename is decided but deliberately bundled into the core session rather than shipped alone.** | **✅ shipped** | **(docs + game select screen)** | **(this commit)** |
| **Phone in-game title vs. "See passage" button** | Title crowds the top-left button on narrow phones. Analysis complete; set aside by the user mid-session (2026-08-09). | 🧊 deferred | — | — |
| E-2b | Simulation harness — MC tuning. Now has a concrete question to answer: how reachable are 12 / 24 / 36 consecutive wins at the v12.1 tier curve? | 🧊 deferred | — | — |
| I | Reward-mode integration. **Scoping largely done 2026-08-22** — see DEFERRED "Reward levels — decisions locked". | 📋 planned | — | — |
| F | Parent for F-1/F-2/F-3. F-1 shipped. F-3 subsumed by tutorial. | 🚧 in flight (1 of 3) | F-1 v12.4 | 1e8be80 |
| G | Campaign hypernova port. | 📋 planned | — | — |
| Research | Match-3 literature survey. | 📋 planned | — | — |

---

## Current platform status

| Platform | Active version | Notes |
|---|---|---|
| **Tablet arcade** | **v12.1** | **2026-08-22 (v12.1): escalating target persists across page loads (`match3_difficultyBonus`) — fixes the verses↔arcade round trip resetting it to base; tiered increments at 12/24/36 wins via one shared helper; "Highest target reached" stat (`match3_highestTarget`) on the end screen + 5-second in-game marker.** 2026-07-04 (v12.0): "📖 Go to Verses" button on the end-game screen (win + loss) → verses.html; bonus moves carry via the shared wallet. Also landed the major-bump graduation (v11.26 → v12.0) marking the in-app tutorial. |
| **Tablet Verses** | **v2.4** | (unchanged) 2026-07-04 (v2.4): in-game "See passage" button. **Not touched by v12.1** — its own `difficultyBonus` at line 2076 is unreachable code, since `VERSES_MODE` is hardcoded `true` at line 881 and verses levels use authored targets. |
| **Campaign tablet** | **v1.28** | (unchanged) |
| **Reward-mode sandbox** | **v1.5** | **2026-08-22 (v1.5): bonus-moves cap 99 → 999, warn 90 → 900 — was silently reducing the shared bonus-move count for every other platform.** Worth auditing in Session I for what stats it already tracks. |
| **Tablet Sim** | **v1.6** | **2026-08-22 (v1.6): same cap fix, in lockstep — hit here before a player did.** |
| **Desktop arcade** | **v12.3** | (unchanged) |
| **Time Attack (desktop)** | **v12.4** | (unchanged) |
| **Phone (arcade)** | **v14.1** | **2026-08-22 (v14.1): escalating target persists (`match3_phone_difficultyBonus`, phone-scoped); 12/24/36 tiers via one shared helper; "Highest target reached" (`match3_phone_highestTarget`) on the end screen + a 5-second marker floated above the target. Difficulty constants remain tablet's, unvalidated on the 12×9 board — see DEFERRED.** 2026-08-09 (v14.0): in-app tutorial ported; combo moved to its own header row; stat-row icons dropped for plain labels; 100dvh + CSS reset. |
| **Phone Verses** | **v2.6** | (unchanged) 2026-08-09 (v2.6): level-select back button no longer overlaps the passage title; preventive safe-area insets; 100dvh. |
| **Phone Verses Sandbox** | **v1.16** | (unchanged) 2026-08-09 (v1.16): lockstep port of the phone-verses v2.6 level-select fix. |
| **Core (`AdminPanel.jsx`, `gameLogic.js`, `versesOrder.js`, `tileDrawing.js`, `Tutorial.jsx`)** | **edit-in-place** | Unchanged 2026-08-22. |

**Storage families (recorded 2026-08-22):** the tablet family (arcade, verses,
campaign, reward mode, sim) shares `match3_bonusMoves` and
`match3_currentRun` / `match3_longestRun`. The phone family (arcade, verses)
uses `match3_phone_bonusMoves` and `match3_phone_*` run keys. High scores and
combo records are unscoped and shared by both — an inconsistency, not a
decision. Full model in `docs/Architecture-2026-08-22.md`.

---

## Rolling "what's next" suggestion

> **▶ IMMEDIATE: play a few tablet arcade rounds and confirm the fix.**
> The check is quick and doesn't need a long run: win one round, note the new
> target, navigate to verses via the end-screen button, come straight back, and
> confirm the target is still raised. Before v12.1 it would have dropped to
> 5,000–6,500. A plain refresh is an equally valid test — the bug was never
> specific to the verses trip.
>
> **▶ AGREED BUILD ORDER (set 2026-08-22):** `VersesPicker` → `PassageSelect`
> rename (bundled) → **core storage consolidation, narrow slice** → reward-mode
> integration (Session I) → simulation harness (E-2b). Reasoning and the full
> scope of each are in DEFERRED → "Agreed task order". The campaign tutorial
> port is deferred by the user until other things are in better shape.
>
> **▶ ALSO IMMEDIATE: playtest the phone arcade, specifically the scoring.**
> User-flagged 2026-08-22, and v14.1 makes it more pressing rather than less.
> Phone arcade runs tablet's difficulty numbers on a **12×9 board (108 tiles)
> against tablet's 12×10 (120)**, and it has **none** of the generosity
> mechanics phone-verses received — neighbour bias, big-turn drops, floor-raise
> rescue, hypernova suppression all live only in phone-verses. From v14.1 the
> escalating ramp climbs at the same rate on the smaller board, so any mismatch
> now compounds across a run instead of staying constant. Nobody has measured
> whether this is actually too hard; the question is open, and the answer is a
> feel judgement first. Levers and the sim option are in DEFERRED →
> "Cross-platform parity".
>
> **▶ Still outstanding from 2026-08-09: confirm phone-verses v2.5 / v1.15 on a
> real iPhone.** Two separate things: (a) whether the Session 15 header clipping
> is genuinely resolved — the diagnosis was ~80% confidence and only sustained
> use settles it; (b) the rebalanced spacing, which is immediately visible.
>
> **▶ NEXT WORK: the tutorial port to campaign** — the last platform, and the
> one that still needs a NEW panel (the progression mini-tutorial, F-3) plus its
> own scoping; see `docs/verses/tutorial-storyboard.md` ("Still open").
>
> Also open: the phone arcade escalating-target bug (same fix, own decision),
> the phone title-vs-"See passage" crowding, and item 7 from the original 7-item
> list — the VersesPicker → "select screen" rename.

---

## Addendum — 2026-08-22 Session 20: escalating target survives navigation (tablet arcade v12.1)

### The bug, and why it was wider than reported

Reported as "switching from verses to arcade resets the target score." The
cause was that `difficultyBonus` — the accumulated raise applied to each new
round's target — was ordinary React state initialized to 0. Both cross-mode
buttons are full page navigations (`window.location.href`), so the whole app is
torn down and the accumulation discarded.

**It was never specific to the verses round trip.** A refresh, a closed tab, or
returning the next day did exactly the same thing. The v12.0 "Go to Verses"
button simply made an existing gap visible by giving players a routine reason to
leave and come back mid-session.

**What made it read as broken rather than merely forgetful** is that the run
counter *was* already persistent (`match3_currentRun`). So after a round trip the
game could report a six-win run beside a beginner target — two features visibly
disagreeing.

### The trap in the fix

Persisting the value is about ten lines, but there are **two** places the target
gets computed. `restartGame` had always added `difficultyBonus`; the initial
`useState` for `levelTarget` deliberately did not, which was correct only while
the value was guaranteed to be 0 at mount. Fixing just `restartGame` would have
left the first round after every page load at the base target with the ramp
appearing on round 2 — the bug half-present, in a form easy to mistake for fixed.

### Design decisions taken during scoping

The fix itself is small; most of the session was design. Recorded in full in
`DESIGN.md` → "Escalating Target (Arcade)" and `DEFERRED.md` → "Reward levels".
The short version, with the reasoning that isn't obvious from the outcome:

- **Permanent persistence, not per-session.** Chosen because the run counter is
  already permanent; a session-scoped target would have produced the same
  contradiction one day later instead of one navigation later.
- **Uncapped, but tapering.** No ceiling; the loss reset is the real limiter.
  The taper stops difficulty accelerating as a run gets long.
- **Boundaries at 12/24/36 rather than 10/20/40** purely so tier steps never
  coincide with the planned every-5-wins reward levels. On 10/20/40 every
  boundary collided; with 12 the first coincidence is win 60.
- **A 100 floor on the later bands**, because the target rounds to the nearest
  100 and smaller increments would be invisible. This also caps how flat the
  curve can ever get — a discovery that killed a proposed fifth, gentler tier.
- **"Highest target reached", not "beaten".** User direction: "beaten" reads as
  gamer-speak. "Reached" also reuses vocabulary the game already teaches on its
  win banner. Semantics are the same — the highest target actually achieved, so
  the round that ends a run does not count.

### Two proposals that were reversed during the session, and why

Worth recording so they aren't re-proposed:

1. **"Highest target reached" on the in-game header, beside high score.** Killed
   on a structural finding: **high score is always ≥ highest target**, because
   winning requires scoring at least the target. Side by side the new stat would
   be permanently the smaller number with no visible reason why, reading as a
   weaker duplicate of the stat next to it. It went to the end screen instead,
   grouped with the run stats where it belongs — it is a run achievement.
2. **A persistent in-game marker on record-breaking rounds.** The target is fixed
   for a whole round, and once a run passes its own best *every* later round
   qualifies — so the badge would light up and never go out for the rest of the
   run. The user's fix (show it ~5 seconds at round start, then clear) restores
   it to being an event, and is now what shipped.

### The stat that turned out not to be redundant

An early concern was that "highest target reached" duplicated "longest run" —
both climb on a win, both reset on a loss. That objection dissolved once reward
levels were scoped: reward wins count toward the run but **not** toward the ramp,
so longest run counts every win including the easy ones, while highest target
only moves on arcade rounds. They now measure genuinely different things: how
long you kept going, versus how hard a fair round you beat.

### Note on the tier lookup and reward levels

The tiers currently key off `currentRun`, which is correct **only while every win
is an arcade win**. Once reward levels count toward the run, the tier lookup will
need its own count of arcade wins or the boundaries will trip early. Flagged in
`DEFERRED.md`; not built, because a second counter identical to the first would
be speculative today.

### Scope held back on purpose

- **Phone arcade has the identical bug** (`match3-v14.0-phone.jsx:1174`). Not
  bundled — whether phone also takes the tiers and the new stat is its own
  decision.
- **`#888` in the tablet header stat row**, below the project's #ccc contrast
  floor on dark backgrounds. Noticed while adding the marker; recorded, not
  touched.

### Verification

`npm run build` clean. Confirmed in the built bundle (`dist/assets/tablet-*.js`),
not just source: `match3_difficultyBonus`, `match3_highestTarget`, "Highest
target reached", "Highest target yet", the `targetRecordFade` keyframes, and the
`v12.1` label. No `DIFFICULTY_INCREMENT_*` constants remain, all four win paths
call `difficultyIncrementFor(currentRun + 1)`, and no stale `v12.0-tablet`
references remain in `src/` or any HTML file. Tier boundaries were checked
against a standalone script: wins 1/11 → 200–500, 12/23 → 200–400, 24/35 →
100–300, 36/50 → 100–200.

**Not verified by playing.** The persistence fix is straightforward and the
build confirms the wiring, but the 5-second marker's timing and colour on the
real header, and the end-screen line's placement, have not been seen running.

## Addendum — 2026-08-22 Session 21: bonus-moves cap divergence (rewardmode v1.5, tablet-sim v1.6)

### The bug

Player-reported: arriving in the reward-mode sandbox from arcade with more than
99 bonus moves and then playing reduced the total to 99, and that reduced number
followed the player back into arcade and verses.

Every platform in the **tablet family** — arcade, verses, campaign, reward mode,
sim — reads and writes one stored count (`match3_bonusMoves`, exported by
`core/AdminPanel.jsx`), so the cap has to be identical in all of them. The phone
family has its own separate pool (`match3_phone_bonusMoves`) and was unaffected. Tablet v11.17 raised it 99 → 999 — and
that comment in the tablet file explicitly warns that a mismatch "would knock a
>99 pool back to 99", which is exactly what happened.

### Why the v11.17 bump missed two files

**A naming split, not a decision.** The T-1/T-2 terminology work renamed
`BANKED_MOVES_CAP` → `BONUS_MOVES_CAP` on most platforms. The reward-mode
sandbox and Tablet Sim were never renamed — the rewardmode file says so
explicitly in its header ("rewardmode is a dev sandbox; full terminology
[rename deferred]"). The v11.17 bump was then applied by searching for the *new*
name, so both files kept the old name and the old value.

### The detail that explains why the sandbox seemed safe

Nothing is lost on arrival. The stored count is read as-is
(`match3-v1.4-tablet-rewardmode.jsx:1504`) and written back as-is (`:1814`). The
cap is applied **only on earning** — `Math.min(prev + newMoves, CAP)` at `:1932`
— so the loss fires at the first 10,000-point threshold reached in the sandbox,
and the persistence effect immediately saves the reduced number for everything
else to read.

Tablet Sim carries the identical code (`:2140` earn clamp, `:2022` write) and was
fixed in the same session before anyone hit it.

**Desktop v12.3, Time Attack v12.4 and Campaign v1.28** all touch the shared
count but define no cap at all, so they cannot reduce it. Safe, but recorded as
its own inconsistency.

### Deferred: move the constant into `core/`

The hand fix closes this instance; the class of bug survives, because the same
limit is written out in seven platform files under two names. `core/` is
edit-in-place and imported by all of them, so one exported constant would make
divergence structurally impossible. User approved in principle and chose to
defer. See DEFERRED.md → "Cross-platform parity".

### Note on the player's save state

The lost moves are unrecoverable — nothing recorded the prior value. The user
declined restoration, noting the surplus only existed because the pre-v12.1
target reset made rounds easy to win repeatedly. **Their `match3_currentRun` is
now over 100**, which interacts with the v12.1 tiers — see the caveat below.

### Verification

`npm run build` clean. Confirmed in both source files and both built bundles:
`BANKED_MOVES_CAP = 999` and `BANKED_MOVES_WARN = 900`, the `v1.5-sandbox` and
`v1.6` in-game labels, and no stale references to the previous filenames in
`src/` or any HTML file. The "Nearing cap — max N" message reads the constant,
so it corrected itself.

**Not verified by playing.** The fix is two constants; the build confirms they
reach the bundles.

## Caveat carried into the next session — a 100+ win run meets the v12.1 tiers

Surfaced 2026-08-22 after v12.1 shipped. The tier lookup picks the increment
band from the current run length, and this player's `match3_currentRun` is over
100 because the pre-v12.1 target reset made rounds repeatedly winnable. So they
sit in **tier 4 (36+ wins, 100–200 per win)** — the gentlest band — while their
`match3_difficultyBonus` starts from roughly zero, since it was never persisted
before v12.1.

The practical effect is that the target will climb about as slowly as the design
allows, from a base start, for a player the game believes is 100 wins deep. Not
a code fault — the tiers are behaving exactly as specified — but the specified
behavior assumes the run counter and the accumulated bonus grew together, which
was never true for data created before v12.1.

**Decision (user, 2026-08-22): leave it and let the run play out.** This is a
beta test run, and a single loss resolves the mismatch on its own — the loss
path zeroes `difficultyBonus`, and `restartGame` zeroes `match3_currentRun` on
the next round. From that point both counters grow together and the tiers
behave exactly as specified. No code change, no manual storage edit.

Two alternatives were considered and rejected: resetting `match3_currentRun` to
0 by hand, and seeding `match3_difficultyBonus` to roughly what a 100-win run
would have accumulated.

**What to expect in the meantime:** a base-level target with the gentlest
increments (100–200 per win), so rounds stay comfortably winnable and the first
loss may be a long way off. The tier behavior worth testing — the taper, and the
step at 12 wins — will not be observable until after that loss and a fresh
climb. A future session reading this should not mistake the slow climb for the
persistence fix having failed; the check for that is whether the target survives
a trip to verses and back, not how fast it rises.

## Addendum — 2026-08-22 Session 22: architecture doc refresh + multi-player direction (docs only)

### Why this happened now

The user described a goal — different people using the app on phones or tablets,
able to connect their phone and tablet progress if they choose — and explicitly
asked for it to be **recorded, not built**. The game is mature enough to be worth
building with that destination in view.

### The finding that changed where it got written

`DESIGN.md` was the user's first suggestion, but per CLAUDE.md that document
covers game mechanics, scoring, unlock gates and terminology. This is
architecture and product direction. `docs/Architecture.md` was the right home —
except the user then asked the sharper question: **does a new session actually
read it?** It did not. CLAUDE.md's session-start list named only the latest
`PROGRESS-*` and `DEFERRED.md`, so anything written to `Architecture.md` would
have sat unread and changed nothing.

Both halves were fixed: the substance went where it is always read, and the
architecture doc was added to the required reading.

### What shipped

- **`docs/Architecture.md` archived as `docs/archive/Architecture-2026-03-20.md`
  and rewritten as `docs/Architecture-2026-08-22.md`.** It was five months old
  and wrong in more places than first reported — not only "core extraction has
  not happened" (it has, five times over) and the retired `phone-341`, but also
  the wrong Vite base path, the wrong deployment URL, a platform list missing
  six directories that now exist, and no mention of `src/` entry files, tests,
  or `content/`. New sections: **Storage & identity model** and **Direction:
  multiple players and optional device linking**.
- **`CLAUDE.md`** — session-start reading list goes from two docs to three,
  adding the latest `Architecture-YYYY-MM-DD.md` with the same rotate-and-archive
  rule as PROGRESS. Also a new writing rule: **do not use invented words**
  without presenting a definition and getting agreement first (prompted by
  "de-staling", which cost a conversation round to decode).
- **`DEFERRED.md`** — new "Multi-player and device linking" section with the
  four stages in dependency order, marked as direction rather than schedule.
- **`DESIGN.md`** — new short section recording that bonus-move pools are per
  device family, which is player-visible and was written down nowhere.

### Correction carried over from Session 21

Session 21's comments, commit message and progress entry named the shared
storage key as `match3_bankedMoves`. **The live key is `match3_bonusMoves`** —
the name was changed in T-3, and the stale name was copied from an older comment
block inside the reward-mode file. Corrected in both platform files and both
docs. Historical mentions inside older version-comment blocks were left alone,
since they correctly describe what was true when written. The Session 21 commit
message itself cannot be corrected without rewriting a pushed commit.

The same session also described the cap as shared by "all platforms". It is
shared by the **tablet family** only; the phone family has its own separate pool
and was never affected. Also corrected.

### Still open, deliberately not bundled

`DESIGN.md` line 41 still reads "The 99 cap exists for UI sanity" in the
Optimize-for-Fun principles. The cap has been 999 since v11.17. Spotted while
editing that file; not changed, because it sits in a section unrelated to this
work. One-word fix whenever someone is next in there.

### Verification

`npm run build` clean (two `.jsx` files were touched, comments only). No live
references to the old `docs/Architecture.md` path exist anywhere outside
`docs/archive/`.

## Addendum — 2026-08-22 Session 23: menu declutter, version history, builder reference

### Two corrections I made during the conversation

Recorded because both were confident and wrong:

1. **"A friend opening the URL would have to guess between ten entries."** The
   admin cards are hidden — `index.html` has `.admin-section { display: none }`
   revealed only by `?admin=1` or a 1.5s long-press on the title. A visitor sees
   seven game cards.
2. **"Bonus moves are shared across everything."** They are shared within a
   device family only. Corrected in Session 22's commit.

### The question that drove this

"If a friend wants to try the game and I give them the URL, do they end up on
the same iteration I play?" The answer separates into three parts, and only one
was a problem:

- **Their progress is their own** — separate device, separate browser storage.
  Nothing mixes. This already works and needs none of the four-stage
  multi-player plan, which addresses two *different* problems (several people on
  one device; one person linking their own phone and tablet).
- **They are always on your latest deployed build** — one URL, one deployment.
  A play-testing property rather than a fault.
- **What they land on was the actual problem** — a version-select screen whose
  cards had grown to as much as 2,300 characters of history each.

### What shipped

- **Ten cards shortened** to `vX.Y · one-line description of the latest change`.
  User direction: keep the version number visible (it's how they confirm they're
  on the latest build), drop the history, and **don't** add a "beta" marker — the
  presence of a version number and a change note implies it.
- **`docs/version-history-2026-08-22.md`** — the removed history verbatim, plus
  all live and local URLs, absorbing `docs/version-urls-2026-08-09.md` (archived).
  Per-version changes and per-version URLs now live in one document.
- **`docs/builder-reference-2026-08-22.md`** — new. Written for the user, not for
  a coding session. Covers sharing links, the tester export flow, both admin
  gestures, how to tell which version is actually live, local dev, and a table of
  every storage key with what it holds.
- **`CLAUDE.md`** — a rule to update the builder reference in the same commit as
  any change it documents. It is the document most likely to drift silently,
  since nothing breaks when it is wrong.
- **`DESIGN.md`** — the stale "The 99 cap exists for UI sanity" line now reads
  999, with the 25 → 99 → 999 history noted.

### The play-tester answer worth remembering

No backend means no automatic telemetry, but the loop already exists and needed
only documenting: a tester opens the game with `?admin=1` (or long-presses the
score for 1.5s), taps **📥 Export JSON**, and sends the file. It carries the
whole `match3_stats` blob and no personal information.

### Verification

`npm run build` clean. All ten card descriptions replaced (checked by count, not
by eye), no `<span class="desc">` left over 110 characters, and the full prior
text preserved in the version-history doc before removal.

## Addendum — 2026-08-22 Session 24: phone escalating-target port (phone arcade v14.1)

Straight port of tablet v12.1. Same bug, same three changes, three differences
worth recording.

### Difference 1 — phone-scoped storage, not shared

`match3_phone_difficultyBonus` and `match3_phone_highestTarget`, matching how
phone already scopes its run counters. This was forced rather than chosen: the
tiers pick their increment band from run length, and phone keeps its own run
totals, so a difficulty value shared with tablet would have two platforms
driving one ramp from two different win counts.

Bonus moves are the counter-example — those are shared within a device family
(`match3_phone_bonusMoves`), so the project does both. Which pattern applies
depends on whether the value is defined in terms of something already scoped.

### Difference 2 — the marker floats instead of sitting inline

On tablet, "↑ Highest target yet" sits beside the target in the header. The
phone's stat row already carries Score / Moves / Target across a ~390px header,
so inline text would wrap. It is absolutely positioned above the target instead,
reusing the pattern the "+1 bonus move" burst already uses in this same file —
no layout space consumed, no chance of shifting the board. User chose this over
an own-line variant (which would make the layout jump twice) and over omitting
it entirely.

### Difference 3 — a correction that changed the picture

The user recalled that the phone version had received generosity mechanics —
increased neighbour-match chance, random special drops — and asked for them to
be documented. They exist and do exactly that, but **they are in phone-verses,
not phone arcade.** Checked across all six active platforms:

| Platform | Neighbour bias | Big-turn drops | Floor-raise | Hypernova suppression |
|---|---|---|---|---|
| phone-verses | ✓ | ✓ | ✓ | ✓ |
| phone-verses-sandbox | ✓ | ✓ | ✓ | ✓ |
| phone arcade | ✗ | ✗ | ✗ | ✗ |
| tablet arcade | ✗ | ✗ | ✗ | ✗ |
| tablet-verses | ✗ | ✗ | ✗ | ✗ |
| reward-mode sandbox | knobs exist, default 0 | | | |

This **strengthened** the playtest concern rather than dissolving it: phone
arcade is the platform with the smaller board *and* none of the compensations.
All four mechanics are now written up in the builder reference, which is where
this is hardest to reconstruct from the code.

The user's call was to proceed as scoped and let playtesting decide, rather than
porting mechanics now — porting is much the larger job and the problem should be
felt first.

### Verification

`npm run build` clean. Confirmed in the built bundle: both phone-scoped keys,
"Highest target reached", "Highest target yet", the `targetRecordFade`
keyframes, and the `v14.1` label. In source: no `DIFFICULTY_INCREMENT_*`
constants remain, all four win paths call `difficultyIncrementFor(currentRun + 1)`
and `recordTargetReached(levelTarget)`, and no stale `v14.0-phone` references
remain in `src/` or any HTML file.

**Not verified by playing** — and on this platform that gap matters more than
usual, since the marker's position and the scoring balance are both things only
play can settle.

## Addendum — 2026-08-22 Session 25: select-screen naming + agreed task order

### The finding

Asked whether the initial landing screen was also called a "picker". It isn't —
`index.html` is plain HTML with no React component, so there is no identifier to
rename. But checking turned up something worse: **the live docs were calling that
one screen five different things** — "launcher", "landing page", "menu",
"version-select screen", "game-select screen" — and several of those were
introduced in this same day's commits.

**"Version select" was also inaccurate**, and it was what the page called itself.
The cards are different platforms (Tablet, Phone, Desktop) and different modes
(Arcade, Verses, Campaign), not versions of one game. The name probably survives
from when they genuinely were successive versions.

### What shipped

Three parallel names, now in the DESIGN.md terminology table:

- **game select** — `index.html`, choose which build to play
- **passage select** — choose a passage (Verses platforms)
- **level select** — choose a level within a passage

`index.html` retitled "M3 — Game Select" with "Select a game to play or test".
The five inconsistent names normalised across the architecture, builder-reference
and version-history docs.

Naming the last two separately matters: calling both "the picker" is what made
the distinction easy to lose, and the Session 17 overlap bug was on the level
select screen specifically.

### The rename is decided but deliberately not shipped

`VersesPicker` → **`PassageSelect`**, chosen over `VersesSelect` because there
are two select screens and the name should say which one it is. Bundled into the
core storage session rather than shipped alone: three platform files carry the
identifier, so shipping it standalone would cost three archives and three version
bumps for a change with no behavior, and the core work touches those same files.

### Task order recorded

Rename (bundled) → core storage slice → reward mode → simulation harness. The
user initially proposed reward mode before core and accepted the reverse: reward
mode needs roughly half a dozen new stored values, and adding those the current
way is half a dozen fresh chances to repeat the divergence that already cost real
bonus moves. Full reasoning in DEFERRED → "Agreed task order".

Campaign tutorial port deferred by the user — not abandoned, but they haven't
played campaign recently and want other things in better shape first.

### Doc-date discrepancy noted

A mid-session system message stated the date was 2026-08-10, while the machine
clock, `date`, and file timestamps all read 2026-08-22. This doc is dated from
the machine clock. If 08-10 was correct, this file needs renaming.
