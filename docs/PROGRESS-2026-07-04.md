# M3 — Progress & Session Roster

**Date:** 2026-07-04
**Purpose:** Living index of planned coding/dev sessions, what's
shipped vs. still to do, plus a narrative summary of key discussions
and decisions that aren't already captured in `DESIGN.md`,
`DEFERRED.md`, `docs/verses/TODO.md`, or `docs/verses/game-design-notes-v2.md`.

> **Personal-doc note:** `docs/claude-code-quickstart.md` is a personal
> reference doc the user maintains about working with Claude Code through
> Cursor's IDE. Not project documentation; future sessions can ignore it.

> **Naming note (Session P-2, 2026-05-02):** The two phone platforms were
> renamed: `phone-418` → `phone` (arcade), `phone-418-verses` →
> `phone-verses`. Phone-341 was retired. Earlier rows/prose use the old
> names where they describe events from that era.

> **Doc structure note (2026-05-25):** Verses docs live under
> `docs/verses/`. Verses-specific planned items live in
> `docs/verses/TODO.md`. DEFERRED.md remains the project-wide deferred
> roster + "Done" archive.

Per-day archived progress docs:
- 2026-07-03 (tutorial: panels 3–6 v11.21, scoring extraction v11.22,
  panel 7 v11.24, panel 8 v11.26 — all 8 shared panels done on tablet
  arcade): `docs/archive/PROGRESS-2026-07-03.md`
- 2026-06-27 (tutorial component + shared panels 1–2, tablet v11.19; first-draft
  panels 3–6): `docs/archive/PROGRESS-2026-06-27.md`
- 2026-06-23 (play-through options + drawing → core/ + tutorial storyboard):
  `docs/archive/PROGRESS-2026-06-23.md`
- 2026-06-22, 06-20, 05-25, and earlier — see `docs/archive/`.

---

## Session roster

Status key: **✅ shipped** · **🚧 in flight** · **📋 planned** · **🧊 deferred**

| Session | Scope | Status | Version shipped | Commit |
|---|---|---|---|---|
| (earlier sessions through 2026-06-27) | See archived progress docs. | ✅ shipped | see archive | see archive |
| Tutorial — Session 3 (2026-07-03) | All 8 shared match panels done on tablet arcade (panels 3–6 v11.21; scoring extraction v11.22; panel 7 v11.24; panel 8 v11.26). | ✅ shipped | tablet v11.26 | merged |
| **Tutorial — Session 4 (2026-07-04)** | **Tutorial PORT to tablet-verses: wired the shared modal in + added the two verses-only panels (V1 reveal-the-verse with Genesis 1:1; V2 target/moves static still). Opt-in "Tutorial" link on the passage-selection screen. First of the ports.** | **✅ shipped** | **tablet-verses v2.0** | **28d59a9** |
| **Tutorial — Session 5 (2026-07-04)** | **Tutorial PORT to phone-verses (second port). Same shared component + same 10 panels; added responsive scaling to `core/Tutorial.jsx` so the fixed-size modal fits a phone viewport (scale 1 on wide screens → tablet unchanged). Reviewed at 390px.** | **✅ shipped** | **phone-verses v2.0** | **efb3ac3** |
| **Verses bug fix — Session 6 (2026-07-04)** | **First-move line-reveal was skipped (second line never appeared) AND single-level games froze at 0 moves with no end screen — one root cause (Begin/restart set the reveal tracker to null while setMoves was a no-op). Fixed across all three verses platforms.** | **✅ shipped** | **tablet-verses v2.1 · phone-verses v2.1 · sandbox v1.11** | **ee37418** |
| **Use bonus moves in verses — Session 7 (2026-07-04)** | **Bonus moves (earned + shown, previously unspendable in verses) are now usable. Loss path: arcade banner drops down to keep playing toward target. Win path: "Use bonus moves" button on the passage screen (keeps 1.5× victory round). Decision 4: loss-path bonus play stays 1×. Tablet-first pilot; phone-verses + sandbox ports pending.** | **🚧 tablet shipped (1 of 3)** | **tablet-verses v2.2** | **827a450** |
| **Arcade button on free-replay — Session 8 (2026-07-04)** | **Parity fix: "Arcade mode" button now also shows on the free-replay end screen (any level replay once a multi-level passage is fully completed, win or fail). Previously arcade vanished on all replays after 100%-ing a passage. Tablet-first; phone-verses + sandbox port rides with the bonus-moves port.** | **🚧 tablet shipped (1 of 3)** | **tablet-verses v2.3** | **4ccbfee** |
| **"See passage" button — Session 9 (2026-07-04)** | **In-game "See passage" button (top-left, right of Back) opens the current level's full passage during play — memorization peek without ending the round. Current level only (whole-game text already reachable via "See entire" + end screen). Tablet-first; phone-verses + sandbox port rides with the bonus-moves port.** | **🚧 tablet shipped (1 of 3)** | **tablet-verses v2.4** | **00370c0** |
| **"Go to Verses" from arcade — Session 10 (2026-07-04)** | **"📖 Go to Verses" button on the tablet arcade end-game screen (win + loss) — reverse of the verses→arcade handoff; navigates to verses.html. Bonus moves carry via the shared wallet (automatic). Lands the v11.26 → v12.0 tutorial-graduation bump. Tablet-first; phone arcade port pending.** | **🚧 tablet shipped (1 of 2 arcades)** | **tablet arcade v12.0** | **ac977b8** |
| **Phone-verses port — Session 11 (2026-07-04)** | **Ported items 3/5/6 (bonus moves, arcade-on-free-replay, See-passage) to phone-verses in one bump. Phone-specific: in-game "← Back" button removed to free the top-left spot for "See passage"; long titles get font tiers (>20→16px, 15–20→18px) + a right-nudge so they clear the button. Sandbox port still pending for these three.** | **🚧 phone-verses shipped (2 of 3 verses platforms)** | **phone-verses v2.2** | **0e5d316** |
| **Phone arcade "Go to Verses" — Session 12 (2026-07-04)** | **"📖 Go to Verses" button on the phone arcade end-game screen (win + loss) → phone-verses.html. Mirror of tablet arcade v12.0. Bonus moves carry via the shared wallet. Completes item 4 across both arcades.** | **✅ shipped (both arcades done)** | **phone arcade v13.7** | **c2b5a98** |
| **Sandbox port — Session 13 (2026-07-04)** | **Ported items 3/5/6 (bonus moves, arcade-on-free-replay, See-passage) to phone-verses-sandbox, mirroring phone-verses v2.2 (in-game Back removed for the See-passage spot; long-title tiers + nudge). Items 3/5/6 now on ALL THREE verses platforms.** | **✅ shipped (all 3 verses platforms done)** | **sandbox v1.12** | **(this commit)** |
| E-2b | Simulation harness — MC tuning. | 🧊 deferred | — | — |
| I | Reward-mode integration. | 📋 planned | — | — |
| F | Parent for F-1/F-2/F-3. F-1 shipped. F-3 subsumed by tutorial. | 🚧 in flight (1 of 3) | F-1 v12.4 | 1e8be80 |
| G | Campaign hypernova port. | 📋 planned | — | — |
| Research | Match-3 literature survey. | 📋 planned | — | — |

---

## Current platform status

| Platform | Active version | Notes |
|---|---|---|
| **Tablet arcade** | **v12.0** | **2026-07-04 (v12.0): "📖 Go to Verses" button added to the end-game screen (win + loss) — reverse of the verses→arcade handoff; navigates to verses.html; bonus moves carry via the shared wallet. This change also lands the long-planned major-bump graduation (v11.26 → v12.0) marking the in-app tutorial — which arrived incrementally as v11.19–v11.26 patch bumps — as a significant part of the game. Graduation note now satisfied.** All 8 shared tutorial panels shipped 2026-07-03. |
| **Tablet Verses** | **v2.4** | **2026-07-04 (v2.4): in-game "See passage" button (top-left, right of Back) opens the current level's full passage during play — memorization peek (Session 9). v2.3: "Arcade mode" button on free-replay end screens (Session 8). v2.2: bonus moves usable in verses (Session 7) — loss-path banner + win-path passage-screen button, loss play stays 1× (decision 4). v2.1: bug fix — first-move reveal skip + single-level end-of-round freeze. v2.0: tutorial port. v2.2/v2.3/v2.4 are tablet-first pilots — phone-verses + sandbox ports pending.** |
| **Campaign tablet** | **v1.28** | (unchanged) |
| **Reward-mode sandbox** | **v1.4** | (unchanged) |
| Tablet Sim | v1.5 | (unchanged) |
| **Desktop arcade** | **v12.3** | (unchanged) |
| **Time Attack (desktop)** | **v12.4** | (unchanged) |
| **Phone (arcade)** | **v13.7** | **2026-07-04 (v13.7): "📖 Go to Verses" button on the end-game screen (win + loss) → phone-verses.html — mirror of tablet arcade v12.0; bonus moves carry via the shared wallet (Session 12). Completes item 4 across both arcades.** |
| **Phone Verses** | **v2.2** | **2026-07-04 (v2.2): port of tablet-verses items 3/5/6 (Session 11) — bonus moves usable in verses (loss-path banner + win-path passage-screen button, loss stays 1×), "Arcade mode" on free-replay end screens (→ phone.html), and a "See passage" button. Phone-specific: in-game Back button removed to free the top-left spot for "See passage"; long titles get font tiers + a right-nudge so they clear the button. v2.1: bug fix (reveal skip + freeze). v2.0: tutorial port. Sandbox port of these three still pending.** |
| **Phone Verses Sandbox** | **v1.12** | **2026-07-04 (v1.12): port of items 3/5/6 (Session 13) — bonus moves usable in verses, "Arcade mode" on free-replay end screens, and a "See passage" button (in-game Back removed for the spot; long-title tiers + nudge). Mirrors phone-verses v2.2. v1.11: bug fix (reveal skip + freeze). Now in lockstep with the two player-facing verses platforms.** |
| **Core (`AdminPanel.jsx`, `gameLogic.js`, `versesOrder.js`, `tileDrawing.js`, `Tutorial.jsx`)** | **edit-in-place** | **2026-07-04: `Tutorial.jsx` gained the two verses-only panels + the engine bits they need — a `reveal` step (fired on the match clear, so the line appears with no delay), a `VerseTextBar` (mirrors the real verses text bar: Georgia serif, two-column reference/content grid, rolling current-plus-2-prior window, per-line reference), and a `TargetMovesStill` (static Target/Moves panel + a replica of the Begin-screen drill selector). Genesis 1:1 text baked in from `content/verses/genesis-1-1-5/game.js`. Session 5 added **responsive scaling**: the modal measures its own natural size and scales down to fit narrow (phone) viewports; scale is 1 on wide screens, so tablet arcade + tablet-verses are visually unchanged (and need no version bump — they inherit the core edit).** |

---

## Rolling "what's next" suggestion

> **▶ STATE OF THE TUTORIAL (read this first):**
> - **ALL 8 SHARED PANELS — shipped on tablet arcade** (v11.26).
> - **PORTS IN PROGRESS.** Build sequence is tablet-first:
>   **tablet-verses (✅ v2.0)** → **phone-verses (✅ v2.0)** →
>   **sandbox (NEXT)** → **campaign** (campaign adds the progression
>   mini-tutorial; F-3 lands there). Both 2026-07-04.
>
> **▶ NEXT WORK: port the tutorial to phone-verses-sandbox.** Same shared
> `core/Tutorial.jsx` (all 10 panels already built + platform-agnostic; the
> responsive scaling from Session 5 already handles the phone viewport). The
> sandbox port is the same easy pattern the two verses ports used: import
> Tutorial, add the top-left "Tutorial" link on the passage-selection screen,
> wire the 10-panel `sections` list, version bump. Then campaign (which is the
> one that still needs a NEW panel — the progression mini-tutorial — plus its
> own scoping; see storyboard "Still open").
>
> The 8 shared panels + the two verses panels are storyboarded in
> **`docs/verses/tutorial-storyboard.md`** ("Build plan — tablet-verses port"
> holds the locked decisions the ports follow).

---

## Addendum — 2026-07-04 Session 4: tutorial port to tablet-verses (v2.0)

First of the tutorial ports. Wired the shared `core/Tutorial.jsx` modal into
tablet-verses and built the two verses-only panels that run after the 8 shared
match panels.

### Scope recovery note

The tablet-verses port had been scoped in a prior session but under-documented.
This session re-derived and locked the decisions with the user, then wrote them
into `docs/verses/tutorial-storyboard.md` ("Build plan — tablet-verses port")
before building, so the ports that follow don't re-litigate them. The five
locked decisions: Genesis 1:1 sample (baked once into `core/`), V1 built to
look like the real verses game, V2 plain-wording (no formula), the "Tutorial"
link on the passage-selection screen, and one major version bump.

### What shipped

- **`core/Tutorial.jsx` (edit-in-place):**
  - **V1 (`verse-reveal`)** — a real match board with the rolling text bar
    above it. Genesis 1:1 (from `content/verses/genesis-1-1-5/game.js`) is
    line-broken exactly as the game does: "In the beginning God" (pre-visible)
    → match reveals "created the heavens and the earth." → match reveals "The
    earth was without form, and void;". The reference shifts Gen. 1:1 → Gen. 1:2
    on the third line, mirroring the game. New engine bits: a `reveal` field on
    the clear step (fires the line the instant the match clears — no delay, per
    user direction) and the `VerseTextBar` renderer.
  - **V2 (`target-moves`)** — a static still (white background, no emojis)
    highlighting the Target Score + Moves Left, plus a replica of the
    Begin-screen "Play this level:" drill selector (1×–4×) to illustrate the
    multiple-play option. Wording stays conceptual (no exact formula).
- **Tablet-verses v1.17 → v2.0:** imports `Tutorial`, adds the opt-in
  "Tutorial" link at the top-left of the passage-selection screen (light pill on
  the purple background), and carries the 10-panel `sections` list. Major bump —
  the tutorial is a significant new screen landing whole. No verses
  gameplay/scoring/board change.

### Review refinements (this session)

- V1: switched from placeholder chunking to the real content-file line breaks
  (the "In the beginning God" break matters — theological point the user
  raised); made the reveal immediate (was a separate delayed step).
- V2: reworded the last sentence to the "for some passages…" phrasing; added
  the drill-selector replica; white background; dropped emojis.

### Verification

- `npm run build` clean after every change. V1's board is authored run-free at
  start; both scripted matches are isolated (match 1 clears cols 1–3, match 2
  uses cols 4–5 which gravity leaves untouched), so the reveal choreography is
  deterministic — same board every replay.

### Versioning note carried forward

Next tablet **arcade** change graduates to **v12.0** (not v11.27) to mark the
tutorial as a significant new screen — see the platform-status table.

### Next

- **Port to phone-verses** (same shared component; wire modal + phone-sized
  "Tutorial" link + version bump), then sandbox, then campaign (adds the
  progression mini-tutorial).

## Addendum — 2026-07-04 Session 5: tutorial port to phone-verses (v2.0) + responsive scaling

Second tutorial port, same day. Two pieces:

- **`core/Tutorial.jsx` (edit-in-place) — responsive scaling.** The modal has a
  fixed natural size (the demo board is a fixed-pixel canvas), which fits a
  tablet but overflows a phone. Added a measure-and-scale step: a `useLayoutEffect`
  reads the box's natural (un-transformed) `offsetWidth/Height` and sets a CSS
  `transform: scale(...)` so the whole box fits `window.innerWidth/Height`
  (recomputed on resize + per panel, since panel heights differ). On wide screens
  the scale is 1, so tablet arcade + tablet-verses are visually identical and
  need no version bump (they inherit the core edit). The overlay got
  `overflow: hidden` so the scaled box never creates scrollbars.
- **Phone-verses v1.11 → v2.0.** Same wiring as tablet-verses: import Tutorial,
  add the opt-in "Tutorial" link at the top-left of the passage-selection screen,
  carry the 10-panel `sections` list. Major bump (matches tablet-verses). No
  verses gameplay/scoring change.

Reviewed at 390px — the modal (board, text bar, controls) fits within the phone
width. `npm run build` clean.

### Next
- **Port to phone-verses-sandbox** (same easy pattern), then **campaign** (needs
  the new progression mini-tutorial panel + its own scoping — see storyboard
  "Still open").

## Addendum — 2026-07-04 Session 6: verses bug fix (v2.1 / v2.1 / v1.11)

Two player-reported bugs turned out to be one root cause. Fixed on all three
verses platforms: tablet-verses v2.0 → v2.1, phone-verses v2.0 → v2.1,
phone-verses-sandbox v1.10 → v1.11. Patch bumps (bug fix, no gameplay/scoring
change).

### The two symptoms
1. **First line reveals, second line doesn't after the first move.** Only the
   first move was affected — later moves revealed lines normally.
2. **Single-level game freezes at 0 moves** — the end-of-round passage modal
   never appears.

### One root cause
Lines reveal one at a time as moves are spent; the reveal effect detects a spent
move by comparing the current move count to `prevMovesRef` (the previous count).
The "Begin game" handler (and the "Play again" restart path) reset
`prevMovesRef` to `null` **and** called `setMoves(versesLevel.moves)`. But at
Begin-click time `moves` already equalled `versesLevel.moves` (seeded by the
mount initializer, or by restart) in the default 1× case — so `setMoves` was a
no-op, the reveal effect never re-ran, and `prevMovesRef` stayed `null` into
play. The first real decrement (12→11) then hit the `prevMovesRef !== null`
guard, so the first reveal was **skipped**. That put every reveal one line
behind for the whole round (symptom 1), and at moves=0 the final line was never
marked revealed — so the end-of-round effect's `revealedChunkIndex < totalChunks
- 1` guard never released and the modal never opened (symptom 2, the freeze).
The bug was masked whenever the player changed the 2–4× drill count on the start
screen, because that genuinely changes the move count and re-runs the effect.

### Fix
Set `prevMovesRef.current = versesLevel.moves` (the move count at play start)
instead of `null`, at both touch points (Begin handler + restart path). The
first move is now correctly detected. The old `null` was defensive but
unnecessary — a move count going *up* (the 0→N mount bump) never triggers a
reveal, which only fires on a *decrease*. `npm run build` clean; no stale
references.

## Addendum — 2026-07-04 Session 7: use bonus moves in verses (tablet-verses v2.2)

Feature: bonus moves — earned and shown in the verses header, but previously
unspendable inside a verses game — can now be used. Tablet-first pilot per
user direction; phone-verses + sandbox ports pending. Reuses the existing
arcade bonus-move machinery (drop-down banner, in-header "End and carry"
button, below-target confirm) wired into the verses end-of-round flow.

### Scoping (decisions locked with user 2026-07-04)
1. **When offered.** Loss (0 moves, below target): the arcade banner drops down
   over the still-visible board instead of jumping to the passage screen —
   "Use bonus moves" resumes play; "End and carry" ends then shows the passage
   screen. Win (0 moves, at/above target): passage screen appears with an added
   "Use bonus moves" button that returns to the board.
2/3. **What they do / completion.** Extra swaps toward the target (passage stays
   fully revealed — the reveal is tied to regular moves, which stay at 0).
   Reaching target on bonus moves completes the level (stars + next-level
   unlock).
4. **Victory round.** A victory round already active on a WIN is kept (leftover
   novas pop at 1.5×). On a LOSS, crossing target during bonus moves completes
   the level but does NOT switch the 1.5× on — it wasn't a victory earned in
   regular play.

### Touch points (tablet-verses v2.1 → v2.2, all in the platform file)
- **Verses end-of-round effect:** added the banner-vs-passage decision (loss →
  banner; still-playing-bonus → wait; win/no-wallet/exhausted → passage screen,
  resetting the using-bonus flag on the way out).
- **3 input-freeze guards:** unfreeze the board during active bonus play
  (moves===0 but wallet remaining).
- **`endLevelCarryBonus`:** added a VERSES branch that opens the passage screen
  (verses' end-of-round) instead of the arcade win/lose gameState it replaces,
  recording completion when target was reached.
- **`resumeBonusFromPassage`:** new handler for the win-path button — closes the
  passage screen, returns to play, leaves the victory round on.
- **Passage-screen button matrix:** added the win-path "Use bonus moves" button
  (only when `won && bonusMoves > 0`).
- **Target→victory-round effect:** loss-path guard — suppress both the 1.5×
  activation AND the "✓ Target reached — 1.5×!" toast when the target is
  crossed during bonus play (the toast text hard-codes "1.5×", so leaving it
  advertised a boost that wasn't applied).

### Review refinement (this session)
- Playtest caught the "1.5×" toast still firing on the loss path when the target
  was hit on a bonus move (the victory-round *scoring* was correctly suppressed,
  but the toast text wasn't). Suppressed the toast on loss-path bonus play too;
  the gold ✓ in the header remains the target-reached signal.

### Next
- **Port to phone-verses and phone-verses-sandbox** (same structural change; the
  end-of-round + bonus code is near-identical across the three). Tracked in
  `docs/verses/TODO.md`.

## Addendum — 2026-07-04 Session 8: Arcade button on free-replay (tablet-verses v2.3)

Small parity fix. The "Arcade mode" button on the passage (end-of-round) screen
appeared on single-level ends and on the first-pass final-level win, but NOT on
free-replay screens. "Free-replay" is the code's `isFreeReplay` flag (true once
`fullCompleted` is set — i.e., the whole multi-level passage has been completed
at least once, unlocking every level for revisiting). Because the button matrix
checks `isFreeReplay` before the final-win branch, once a passage was 100%'d,
EVERY replay (including replaying the final level) fell into the free-replay
branch — which had no arcade button. So arcade disappeared entirely on replays
after full completion.

Fix: added the "Arcade mode" button to the `isFreeReplay` branch, unconditionally
(win OR fail) — the passage is already fully won either way, so the handoff is
always valid there (user direction 2026-07-04). Reuses the existing shared-wallet
navigation to `tablet.html`. No other branch changed: first-pass non-final wins
and first-pass fails still show no arcade button, per user scope (final-level
only, not on mid-passage screens). One `buttons.push` in one branch.

### Next
- The phone-verses + sandbox port of the bonus-moves feature (Session 7) will
  carry this one-line arcade-button addition along, so all three verses
  platforms end up consistent.

## Addendum — 2026-07-04 Session 9: "See passage" button (tablet-verses v2.4)

Added an in-game "See passage" button so a player can peek at the current
level's full passage mid-play without ending the round. Placement (user
direction 2026-07-04): top-left, on the same row as the "← Back" button and
just to its right (first tried below Back, but that overlapped the score).
Small accent-colored pill.

Opens a popup rendering the CURRENT LEVEL's chunks (`versesLevel.flatChunks`)
in the same Georgia two-column layout as the start/end passage modals, with a
single Close. Current level only, per user direction — the whole-game text is
already reachable via "See entire <title>" (final-win / level-select) and the
end-of-round screen, so mid-play only the current level is needed. The
full-screen overlay (zIndex 2000) freezes board input while open. New state:
`showLevelPassage`; hidden while the start/end passage modals are up.

### Next
- Ports (phone-verses, sandbox) for Sessions 7–9 batch together — the
  bonus-moves feature is the large one; the arcade-button (S8) and "See
  passage" (S9) additions ride along.

## Addendum — 2026-07-04 Session 10: "Go to Verses" from arcade + v12.0 graduation (tablet arcade)

Added the return path: a "📖 Go to Verses" button on the tablet arcade
end-game screen (win AND loss), the reverse of the "Arcade mode" handoff verses
already had. It navigates to `verses.html` (the verses selection screen). Always
shown — serves both the player who arrived from verses and the arcade-first
player who wants to try verses (user decision). Same button styling as "Play
Again"; sits right beside it.

**Bonus moves carry automatically** — tablet arcade and tablet-verses share one
persistent wallet (core `BONUS_MOVES_KEY`, read fresh at mount on both sides), so
there was nothing to transfer; the "carrying" is inherent.

**v12.0 graduation.** This change also lands the long-planned major-bump: the
in-app tutorial arrived as incremental v11.19–v11.26 patch bumps, and per user
direction the next tablet-arcade change graduates the version to v12.0 to mark
the tutorial as a significant part of the game. This is that change. The
standing "next-touch graduates to v12.0" note is now satisfied.

### Next
- **Phone arcade** needs the same "Go to Verses" button (navigating to
  `phone-verses.html`) — the phone-side reverse handoff. Pending, alongside the
  verses phone-verses/sandbox ports.

## Addendum — 2026-07-04 Session 11: phone-verses port of items 3/5/6 (phone-verses v2.2)

Ported the three tablet-verses features to phone-verses in one bump
(v2.1 → v2.2): use bonus moves (v2.2), Arcade mode on free-replay (v2.3), and
the "See passage" button (v2.4). Structurally identical to tablet-verses; the
end-of-round + bonus + button-matrix code is a near-verbatim mirror. Two
platform-specific differences from tablet:

- **Arcade destination is `phone.html`** (the phone arcade), not tablet.html.
- **"See passage" placement.** Phone's header row (Back · title · dark/light
  toggle) had no room for the pill. Decision (user, 2026-07-04): **remove the
  in-game "← Back" button on phone** and put "See passage" in that top-left
  spot. Mid-game back is rarely needed; to leave, finish the level (moves are
  limited) and use the end screen's "Back to passage selections". `onBack` is
  still used by the start-of-round modal's Back. **Tablet-verses keeps its
  in-game Back** (it has the room, and there "See passage" sits to Back's right).
- **Long-title handling (phone).** A long centered title ran under the top-left
  "See passage" button. Fix: char-length tiers on the title font (> 20 → 16px;
  15–20 → 18px; ≤ 14 → 20px) plus a 48px left-pad right-nudge for titles over
  14 chars (shift into the free right-side space rather than squeeze the left).
  Calibrated against "1 Thessalonians 4:13–18" (23 chars, the widest) and
  "Isaiah 52:13–53:12" (18). Titles may read slightly off-center now — accepted;
  revisit if it bothers.

### Future option (noted for later, user direction 2026-07-04)
- **Make the rolling passage text bar itself tappable/expandable** to show more
  lines of the passage — a zero-chrome alternative or addition to the "See
  passage" button if more interface is ever needed, or as another way to see
  more of the passage at once.

### Next
- **phone-verses-sandbox** port of items 3/5/6 (same three changes), then the
  **phone arcade** "Go to Verses" button (item 4 phone side).

## Addendum — 2026-07-04 Session 12: phone arcade "Go to Verses" (phone arcade v13.7)

Mirror of the tablet arcade v12.0 change on the phone arcade: a "📖 Go to
Verses" button on the end-game screen (win + loss) that navigates to
`phone-verses.html`. Always shown; same styling as "Play Again". Bonus moves
carry automatically — phone arcade and phone-verses share one wallet
(`PHONE_BONUS_MOVES_KEY`). Patch bump v13.6 → v13.7 (no graduation note — that
was tablet-specific). **Item 4 is now complete on both arcades.**

### Remaining from the original 7-item list
- **Item 7** — VersesPicker → "select screen" rename, deferred to a later session.

## Addendum — 2026-07-04 Session 13: sandbox port of items 3/5/6 (sandbox v1.12)

Ported the three verses features to phone-verses-sandbox (v1.11 → v1.12),
mirroring phone-verses v2.2 verbatim in structure: use bonus moves (loss-path
banner + win-path passage-screen button, loss stays 1× with toast suppressed),
Arcade mode on free-replay end screens (→ phone.html), and the "See passage"
button (in-game "← Back" removed to free the top-left spot; long-title font
tiers + right-nudge; "Level N of M" → "N/M"). Same six/three touch points as
the phone-verses port. `npm run build` clean.

**Items 3, 5, and 6 are now live on all three verses platforms** (tablet-verses,
phone-verses, phone-verses-sandbox) — fully in lockstep. Item 4 is done on both
arcades. Of the original 7-item list, only the **item 7 rename** remains
(deferred by the user to a later session).
