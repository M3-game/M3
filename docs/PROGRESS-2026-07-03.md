# M3 — Progress & Session Roster

**Date:** 2026-07-03
**Purpose:** Living index of planned coding/dev sessions, what's
shipped vs. still to do, plus a narrative summary of key discussions
and decisions that aren't already captured in `DESIGN.md`,
`DEFERRED.md`, `docs/verses/TODO.md`, or `docs/verses/game-design-notes-v2.md`.

> **Personal-doc note:** `docs/claude-code-quickstart.md` is a personal
> reference doc the user maintains about working with Claude Code through
> Cursor's IDE. Not project documentation; future sessions can ignore it.

> **Naming note (Session P-2, 2026-05-02):** The two phone platforms were
> renamed: `phone-418` → `phone` (arcade), `phone-418-verses` →
> `phone-verses`. Phone-341 was retired. Earlier rows/prose use the old
> names where they describe events from that era.

> **Doc structure note (2026-05-25):** Verses docs live under
> `docs/verses/`. Verses-specific planned items live in
> `docs/verses/TODO.md`. DEFERRED.md remains the project-wide deferred
> roster + "Done" archive.

Per-day archived progress docs:
- 2026-06-27 (tutorial component + shared panels 1–2, tablet v11.19; first-draft
  panels 3–6): `docs/archive/PROGRESS-2026-06-27.md`
- 2026-06-23 (play-through options + drawing → core/ + tutorial storyboard):
  `docs/archive/PROGRESS-2026-06-23.md`
- 2026-06-22, 06-20, 05-25, and earlier — see `docs/archive/`.

---

## Session roster

Status key: **✅ shipped** · **🚧 in flight** · **📋 planned** · **🧊 deferred**

| Session | Scope | Status | Version shipped | Commit |
|---|---|---|---|---|
| (earlier sessions through 2026-06-23) | See archived progress docs. | ✅ shipped | see archive | see archive |
| Drawing code → `core/` — Session 1 (2026-06-23) | Extract tile-drawing into `core/tileDrawing.js`; migrate tablet arcade. Tutorial foundation (verses TODO #4). | ✅ shipped | tablet v11.18 | 20b3654 |
| Tutorial — Session 2 (2026-06-27) | Portable `core/Tutorial.jsx` + opt-in Tutorial button on tablet arcade. Shared match panels 1–2 (basic match, line special). | ✅ shipped (2 of 8 panels) | tablet v11.19 | c7ea9fe |
| **Tutorial — Session 3 (2026-07-03)** | **Shared panels 3–6 (bomb, cross, supernova, hypernova) reviewed, refined & merged to main. 6 of 8 shared panels done.** | **✅ shipped (6 of 8 panels)** | **tablet v11.21** | **(this commit)** |
| E-2b | Simulation harness — MC tuning. | 🧊 deferred | — | — |
| I | Reward-mode integration. | 📋 planned | — | — |
| F | Parent for F-1/F-2/F-3. F-1 shipped. F-3 subsumed by tutorial. | 🚧 in flight (1 of 3) | F-1 v12.4 | 1e8be80 |
| G | Campaign hypernova port. | 📋 planned | — | — |
| Research | Match-3 literature survey. | 📋 planned | — | — |

---

## Current platform status

| Platform | Active version | Notes |
|---|---|---|
| **Tablet arcade** | **v11.21** | **2026-07-03 (Session 3): tutorial shared panels 3–6 (bomb, cross, supernova, hypernova) reviewed, refined & merged. Refinements live in `core/Tutorial.jsx` (blast refill spreads all six colors; panel-4 cross board fixes). `sections` list carries all six shared panels. No arcade gameplay/scoring/board-render change. (v11.19: opt-in Tutorial button + panels 1–2. v11.18: tile-drawing → core/.)** |
| **Campaign tablet** | **v1.28** | (unchanged) |
| **Reward-mode sandbox** | **v1.4** | (unchanged) |
| Tablet Sim | v1.5 | (unchanged) |
| **Tablet Verses** | **v1.17** | (unchanged) |
| **Desktop arcade** | **v12.3** | (unchanged) |
| **Time Attack (desktop)** | **v12.4** | (unchanged) |
| **Phone (arcade)** | **v13.6** | (unchanged) |
| **Phone Verses** | **v1.11** | (unchanged) |
| **Phone Verses Sandbox** | **v1.10** | (unchanged) |
| **Core (`AdminPanel.jsx`, `gameLogic.js`, `versesOrder.js`, `tileDrawing.js`, `Tutorial.jsx`)** | **edit-in-place** | **2026-07-03: `Tutorial.jsx` panels 3–6 refined (refill variety + panel-4 board). (06-27: `Tutorial.jsx` added — the portable tutorial component; paints with `core/tileDrawing.js` for pixel-exact tiles.)** |

---

## Rolling "what's next" suggestion

> **▶ STATE OF THE TUTORIAL (read this first):**
> - **Panels 1–6 — SHIPPED on `main`** (tablet v11.21). Panels 1–2 (basic
>   match, line special) and 3–6 (bomb, cross, supernova, hypernova). All
>   reviewed & approved.
> - **Panel 7 (multipliers) — NOT BUILT.** Needs the **second core extraction**
>   first (the scoring functions `getMultiplier`/`getCascadeMultiplier` + point
>   values into `core/gameLogic.js`), so tutorial and game can never drift.
> - **Panel 8 (fusion) — NOT BUILT.** The multi-tier finale (+700 → +1500 →
>   +8000 ladder).
>
> All 8 shared panels are storyboarded in
> **`docs/verses/tutorial-storyboard.md`** (read first). **▶ NEXT WORK:** build
> panel 7 (starting with the scoring extraction) then panel 8.
>
> Then the ports (one platform each, tablet-first): tablet-verses (adds the
> verses-only static panels) → phone-verses → sandbox → campaign (adds the
> progression mini-tutorial; F-3 lands here). See verses TODO #4 build sequence.

---

## Addendum — 2026-07-03 Session 3: tutorial panels 3–6 reviewed, refined & merged (tablet v11.21)

Reviewed the first-draft panels 3–6 (bomb, cross, supernova, hypernova) with the
user on the `tutorial-panels-3-6` branch, refined them, then merged to `main`.

### What shipped

- **`core/Tutorial.jsx` (edit-in-place):**
  - **Refill variety.** After any blast, the refill now starts each new cell's
    color search at a position-derived offset and cycles through all six tile
    types (still rejecting any that would complete a run). This fixes the
    red/blue wash the earlier greedy scan-from-red produced on the big-blast
    panels (3–6). Still deterministic — same board every replay.
  - **Panel 4 (cross) board.** Two tiles recolored. (3,2) green→red removes a
    4-match alternative to the taught 3-match trigger: the cross is green, so a
    green sliding into the trigger cell would line up a green 4 (a better-looking
    move we must not leave on screen). (2,1) orange→blue stops the trigger swap
    from leaving an uncleared incidental row-2 match — the tile the swap
    displaces upward no longer lands beside a same-color pair.
  - Panels 3, 5, 6 were sim-verified free of the same incidental-match issue:
    they make their special with a **vertical** drag, so the displaced tile
    lands with no matching neighbors. Panel 4 was the only one affected (its
    make-move is horizontal).
- **Tablet arcade v11.20 (draft) → v11.21:** finalized — draft markers removed
  from the header comment, in-game label, and `index.html`. `sections` unchanged
  (all six shared panels). No arcade gameplay/scoring/board-render change.

### Verification

- `npm run build` clean. Every panel's full make→use sequence was replayed in a
  node simulation: run-free at start and after each drop, the make produces
  exactly the intended special, and the trigger swap yields exactly one match
  (the intended one) fully inside the blast footprint — no leftovers.

### Not addressed / next session

- Panel 7 (multipliers — needs the `core/gameLogic.js` scoring extraction) and
  panel 8 (fusion finale).
- Real-device playtest of the tutorial → user.
